# AÇIK İŞLER DEFTERİ

> **Bu defterin varlık sebebi bir hata.** KARAR A25 (`docs/DESKTOP-ARCHITECTURE.md` EK 4) verildi, tutanağa geçti, ve `docs/DESKTOP-THREAT-MODEL.md`'nin §9/2 maddesi "kapandı" sayıldı — ama **kodda hiçbir karşılığı yoktu**. Bağımsız bir denetim (RISK-1) bunu yakaladı.
>
> Kök neden: AÇIK maddeler üç belgeye dağılmıştı ve hiçbirinde **"kodda var mı?"** sütunu yoktu. Karar yazmak, iş bitirmekle karıştırıldı.
>
> **Kural:** bir madde ancak **üç sütunu da ✅ olduğunda** kapanır. Karar ✅ + kod ❌ = **AÇIK**.

Son güncelleme: 2026-09-01 (dördüncü tur) · **F6 ve F7 KAPANDI** · **CI ilk kez uçtan uca YEŞİL** (10/10 iş, `release smoke` dahil — defter O115) · **F8/1 + F8/2 + F8/3 teslim edildi**
> Bu turda kapananlar: O4, O12, O26, O27, O42, O53, O60, O66, O70, O72, O79, O85, O87, O91, O92, O93, O95, O102, O104, O105, O108, O110, O113 · yeni açılanlar: O100, O101, O106→kapandı, O107→kapandı, O109, O111, O112, O114
> Kapı: backend **1481** · crate **174** · src-tauri **186** · vitest **388** · check:data **138/138** · check:commands **33/33** · check:errors **27/27**
>
> **Bu turun üç dersi:**
> 1. **Bir defter satırının "açık" demesi, işin yapılmadığı anlamına gelmez.** Bağımsız denetim altı satırın kodun gerisinde kaldığını buldu (O53, O66, O70, O72, O79, O93). Yanlış "açık", yanlış "kapalı" kadar pahalıdır: birincisi yapılmış işi tekrar ettirir.
> 2. **Bir kontrolörün yeşili, ölçtüğü şey kadar iyidir.** `i18n:dead-keys` §8'in 17 anahtarını 17→0 gösterdi; dördünün hiç çağrı yeri yoktu — önek eşleşmesi bir aileyi toptan canlı sayıyordu (O109). Pano tarayıcısı yalnız `tracing::` içeren **tek satırlara** bakıyordu (§9/6). `check-command-wiring`'in `CONTRACT`'ı §6.2'nin gerisindeydi ve iki teslim edilmemiş komutu göremiyordu (O114).
> 3. **"Kapandı" ile "çalışıyor" arasındaki mesafe ölçülmeden kapanmaz.** O12 kapandı sayıldı; `createUpdaterArtifacts` olmadığı için updater **hiçbir zaman var olmayacak bir manifest'i** işaret ediyordu (O108). Ölçüm, bundle çıktısına bakmak kadar basitti.

---

## Sütunların anlamı

| Sütun | ✅ ne demek |
|---|---|
| **Karar** | Bağlayıcı bir kararla çözülmüş; ne yapılacağı belirsiz değil |
| **Kod** | Kararın karşılığı üretim kodunda var, `dosya:satır` ile gösterilebilir |
| **Test** | Davranışı kilitleyen bir test/kontrol var; bozulursa kırmızı verir |

---

## 1. AÇIK — kararı var, kodu yok

| # | Madde | Karar | Kod | Test | Kanıt / not |
|---|---|---|---|---|---|
| ~~**O1**~~ | **A25 — 403 `USER_DEACTIVATED` → wipe** — **KAPANDI 2026-08-31** | ✅ EK 4 | ✅ `sync/mod.rs:1072` (`status==403 && code==USER_DEACTIVATED`), `protocol.rs:372`, `error.rs` yapısal `code` | ✅ `tests/wire_contract.rs` 3 test — biri pozitif, **ikisi negatif kontrol** (`a_plain_403_does_not_wipe_anything`, `a_bare_401_still_keeps_the_outbox`) | `transport.rs:137` 403'ü ayrıştırmadan `SyncError::Protocol`'e katlıyor; `USER_DEACTIVATED` desktop kodunda **hiç geçmiyor** (grep 0). `handle_auth_lost` yalnız 401'de. **Bugünkü davranış karardan da kötü:** deaktive kullanıcı oturumu düşmeden "protocol error" görüyor. §9/2 AÇIK, F6 kapatılamaz. |
| ~~**O2**~~ | **A26 SLA alanları — KAPANDI 2026-08-31, uçtan uca** | ✅ EK 4 | ✅ üç halka birden: sunucu (`attachTicketSla()`), ayna (`0002` migration, 4 kolon), istemci (`mapTicket()` — `null`=SLA yok, `0`=süre doldu ayrımı korunuyor) | ✅ Sunucu: `SyncPullTicketSlaTest` 4 senaryo. Taşıma: `ticket_sla_fields_survive_the_upsert_round_trip` + `a_null_sla_remaining_seconds_survives_as_sql_null_not_zero`. Mapper: 7 test, 2'si negatif kontrol. |
| ~~**O3**~~ | **TM-F2 — `sync_deletions` sahip kapsamı** — **KAPANDI 2026-08-31** | ✅ | ✅ `SyncDeletionObserver.php:51`, `SyncPullService.php:542` (`owner_key` MATCH, "başkasınınki değil" değil), migration `2026_09_01_100010` | ✅ | `owner_key` repoda hiç yok (grep 0). Bir kullanıcı başkasının silinmiş bildirim uuid'lerini görebiliyor. F6 buna bloklu. |
| ~~**O4**~~ | **R4 — hardcoded localhost CSP — KAPANDI 2026-09-01, üç katmanlı fail-closed** | ✅ | ✅ (1) `tauri.conf.json:28` commit'li CSP artık **her şeyi reddeden sentinel** (`x-syncra-placeholder-csp`), sarmalayıcısız paket boş webview üretir — sessiz "çalışır gibi" hâli yok · (2) `build.rs:62-137` **release profilinde dört ayrı red**: `SYNCRA_API_URL` yok / mutlak http(s) değil / `TAURI_CONFIG` overlay'i yok / overlay CSP'si gömülü URL'in origin'ini içermiyor · (3) `build.rs:41-49` debug'ta panik yerine `cargo:warning` (CI `desktop-rust` job'ı etkilenmiyor) | ✅ teknik lider negatif kontrolü koştu: env'siz `cargo build --release -p syncra-desktop` → `RELEASE BUILD REFUSED: SYNCRA_API_URL is not set`, **exit 101**; sarmalayıcıdan `npm run tauri -- build` → EXIT=0 ve üretilen exe'de placeholder **yok**, gerçek origin **var** | Aşağısı tarihçedir: `tauri.conf.json:28` `http://localhost:8000` taşıyor; doğru CSP yalnız `scripts/tauri.mjs` sarmalayıcısından geçince üretiliyor. `npx tauri build` doğrudan çağrılırsa **sessizce localhost-CSP'li paket** çıkar. |
| ~~**O5**~~ | **komut adı `stats` vs `storage_stats` — KAPANDI 2026-08-31** | ✅ sözleşme adı kazandı | ✅ `storage.rs:19`, `lib.rs:106`, `commands.ts:150` üçü de `storage_stats` | ✅ `npm run check:commands` — dört kaynağı (TS `invoke` · Rust fn · `generate_handler!` · §6.2) karşılaştırır. Teknik lider bozup ölçtü: **bozukken exit 1, düzeltilince exit 0**. |

| ~~**O25**~~ | **5xx artık `Offline` değil — KAPANDI 2026-08-31** | ✅ | ✅ `transport.rs` 5xx → `SyncError::Server`; `sync/mod.rs::is_server_5xx()` geri çekilmeyi **koruyor** (5xx hâlâ yeniden deneniyor, 4xx girmiyor); `commands/mod.rs` kodsuz 5xx'i `SERVER_ERROR`'a düşürüyor, sunucunun kendi kodu varsa koruyor | ✅ `tests/server_errors.rs` 6 test + `commands/mod.rs` 4 birim testi. Negatif kontroller: gerçekten erişilemez sunucu hâlâ `Offline`; 422 ve 401 online bayrağını düşürmüyor |
| ~~**O26**~~ | **`wiped_local_only` özel anahtarı — KAPANDI 2026-09-01** | ✅ | ✅ `desktop.logout.wipedLocalOnly` tr/en/de/fr dördünde; `desktop/src/platform/auth.ts:637-641` `warnLocalOnlyLogout` artık tek anahtardan okuyor, `devices.revokeError` + `devices.subtitle` ödüncü kaldırıldı | ✅ `npm run i18n:check` EXIT=0 (28 namespace, tam parite) — teknik lider koştu | Aşağısı tarihçedir: Cümle `desktop:devices.revokeError` + `desktop:devices.subtitle` birleşiminden kuruluyor — dört dilde ve doğru, ama ödünç. Önerilen: `desktop.logout.wipedLocalOnly`. |
| ~~**O27**~~ | **`SYNCRA_API_URL` — İKİNCİ YARI DA KAPANDI 2026-09-01** | ✅ | ✅ `scripts/tauri.mjs` CSP ile **aynı** kaynaktan (`frontend/.env` → `VITE_API_URL`) türetip cargo ortamına enjekte ediyor; açık `SYNCRA_API_URL` hâlâ kazanıyor. `build.rs`'e `cargo:rerun-if-env-changed=SYNCRA_API_URL` eklendi — **`option_env!` tek başına yeniden derlemeyi tetiklemiyordu**. **Yeni:** türetme mantığı `scripts/build-env.mjs`'te **tek kaynağa** indirildi (checker ile sarmalayıcı ayrışamasın), `scripts/check-release-host.mjs` + `npm run check:release-host` eklendi, `desktop-release.yml:160-192` `.env`'i `secrets.DESKTOP_RELEASE_ENV`'den yazıp assert'i **build'den önce** koşuyor | ✅ Teknik lider iki yönü de koşturdu: yerel `.env` (127.0.0.1 + localhost) → **EXIT=1**, iki bulguyu isimle raporluyor; üretim host'u override'ıyla → **EXIT=0**. Ayrıca `[::1]`, `.local`, `0.0.0.0`, parse edilemeyen URL varyantları da reddediliyor | `ipc: http://ipc.localhost` bilinçle muaf — Tauri'nin kendi iç kaynağı, olmasa kapı koşulsuz kırmızı olurdu. Gerçek çıktıyla doğrulandı: varsayılan → `http://localhost:8000/api/`, override → `https://crm.example.com/api/`. **Kalan (F7):** release workflow'una "URL localhost/`::1` ise FAIL" assert'i (O4 ile birlikte). |
| ~~**O28**~~ | **Tehdit modeli defterle eşitlendi — KAPANDI 2026-08-31** | 14 blok güncellendi: §3/2 (A25 çözümü), §3/9 (`logging.rs` iki adım), STRIDE I2/I5/E3 satırları, TM-F1/F2/F3 ✅, §6 özet satırı 9, F6 iş listesi. **TM-F1 keşif turunda "açık kalsın" denmişti; kodda doğruladım ve kapattım** — sunucu `AuthService.php:355` gerçekten `403 USER_DEACTIVATED` dönüyor, istemci `sync/mod.rs:1072` o sinyalde wipe ediyor, yani TM-F1'in (b) seçeneği uygulanmış. Ayrıca tabloda **kaçırılmamış bir boru işareti** bulundu (`reason: deactivated\|deleted`) — satır markdown'da bir kolon kaymış hâlde duruyordu, düzeltildi. |

| ~~**O29**~~ | **`data::get` ölü komutu — KAPANDI 2026-08-31 (silindi)** | ✅ | ✅ komut + `generate_handler!` + kontrolör `CONTRACT`'ı birlikte silindi; §6.2'den de düşürüldü | ✅ `check:commands` → `dead commands: 0`. Silme öncesi tüketici yokluğu grep'le kanıtlandı (`rowById` yerel `query` yolundan gidiyor) |
| ~~**O30**~~ | **`deals:form.companyLabel` — KAPANDI 2026-08-31 (silindi)** | ✅ | ✅ dört dilden birden silindi | ✅ `i18n:dead-keys` 96 → **95**; `BoardFilters.tsx`'in kullandığı `board.filters.companyLabel` korundu |

| ~~**O32**~~ | **CI Linux: `libappindicator3-dev` çakışması — KAPANDI 2026-08-31, CI ile DOĞRULANDI** | ✅ | ✅ 3 yerden kaldırıldı | ✅ `rust workspace (ubuntu-24.04)` işi **success**. Önceki koşumda apt exit 100 ile ölüyordu; şimdi derleyip test ediyor. |
| ~~**O33**~~ | **CI backend: Redis servisi — KAPANDI 2026-08-31, CI ile DOĞRULANDI** | ✅ | ✅ `redis:7-alpine` servisi + `REDIS_*` env | ✅ `backend (php artisan test)` işi **success** (önceki koşumda 15 kırmızı, hepsi `Connection refused [tcp://127.0.0.1:6379]`). Sürücüleri `sync`/`array`'e çevirme yolu reddedildi — iş yeşile dönerdi ama üretimden farklı bir yürütme modelini test ederdi. |

| ~~**O34**~~ | **Release runner `ubuntu-22.04` — KAPANDI 2026-09-02, ÖLÇÜMLE (tercihle değil)** | ✅ | ✅ `desktop-release.yml` matrisi **24.04**'e taşındı, CI ile aynı taban | ✅ İlk gerçek release koşumu (run 33564934573) kanıt: 22.04'te `error: could not compile \`libspa\` (lib) due to 7 previous errors` — E0308/E0425/E0560/E0609, yani Rust crate'i jammy'nin `libpipewire-0.3-dev` başlıklarında **olmayan alanları** okuyor | Madde *"ürün kararı, açık bir çağrı bekliyor"* diye duruyordu ve gerekçesi gerçekti: release runner'ın glibc'si dağıtılan binary'nin **tabanı** olur (22.04 = 2.35, 24.04 = 2.39), yükseltmek eski dağıtımları düşürür. **Ama ölçüm takasın ikinci tarafını yok etti:** 22.04 bu projeyi **hiç derleyemiyor**, yani korunacak bir 22.04 binary'si yok. Ayrıca eski yorumun kendi saydığı bedel de kalktı — CI ve release artık aynı tabanda derliyor, Linux kırılması tag'de değil CI'da görünüyor. |

| ~~**O35**~~ | **Yerel şema SLA kolonlarını aynalamıyordu — KAPANDI 2026-08-31** | ✅ | ✅ `migrations/0002_ticket_sla_fields.sql` + **migration zinciri** (`MIGRATIONS: &[(i32, &str)]`, sürüm aralığına göre sırayla uygulama) | ✅ `existing_v1_database_gets_migration_0002_applied_on_next_open` — teknik lider adıyla koşturdu, `ok`. Negatif kontrol `an_unmirrored_key_is_still_silently_dropped` da geçiyor: kolon eklemek `upsert`'in bilinçli sessiz-düşürme güvenliğini bozmadı. |

| ~~**O36**~~ | **Pano lookup'ları → DataSource — KAPANDI 2026-08-31** | ✅ `deals.stages()` + `deals.ownerOptions()` (126 → **128** metot) | ✅ `boardApi.ts`'te doğrudan veri çeken axios çağrısı kalmadı; masaüstünde aşamalar `pipeline_stages`, sahip/firma `user_list`/`company_list` ile aynadan; `board()` ve `stages()` aynı yardımcıyı paylaşıyor | ✅ `check-data-wiring` 128/128, EXIT=0. `isForbidden` ve `retry: false` iki hook'ta da değiştirilmeden korundu (`boardApi.ts:173/175`, `:195/197`). |
| ~~**O37**~~ | **`pipeline_stages.name_key` — KAPANDI 2026-08-31** | ✅ | ✅ aynı `0002` migration + `mapPipelineStage` artık satırdan okuyor | ✅ `pipeline_stage_name_key_survives_the_upsert_round_trip` + null varyantı. Masaüstünde aşama başlıkları yine `enums:pipelineStage.<name_key>` ile çevriliyor. |
| **O38** | **Kontrolörlerin satır-sonu kırılganlığı** | ✅ | ✅ düzeltildi | ⚠️ | `check-data-wiring.mjs`'in manifest regex'i `\n}\n`'e sabitliydi ve CRLF ağaçta **çöküyordu** (`DATA_METHOD_MANIFEST not found`). <br><br>**Bu bir şerit hatası değildi, gizli bir kusurdu:** `core.autocrlf=true` ve index **%100 LF** (189 dosya, sıfır CRLF) — yani git Windows'ta çalışma ağacına **kasten** CRLF veriyor. Kontrolör hiçbir zaman temiz bir Windows clone'unda güvenilir değildi; bugüne kadar yalnız o dosyaya dokunulmadığı için ortaya çıkmamıştı. Regex `\r?\n` toleranslı yapıldı. Diğer dört kontrolör ampirik olarak CRLF ağaçta yeşil. <br><br>**Ders:** bir şeridin "kontrolör yeşil" raporu, o kontrolörü teknik lider yeniden koşmadan kanıt değildir — bu turda tam olarak bu oldu, rapor yeşildi, tekrarlanabilir değildi. |

| ~~**O39**~~ | **Migration atomikliği — KAPANDI 2026-08-31** | ✅ | ✅ `migrate_with()` her migration için `unchecked_transaction()` açıyor; `execute_batch(sql)` **ve** `pragma_update(user_version)` aynı işlemde, sonra `commit()`. Hata olursa `?` erken döner, `tx` düşerken rollback atar. Aynı dosyadaki `wipe()` deseniyle tutarlı. | ✅ `a_failing_migration_rolls_back_and_can_be_retried_after_the_fix` — teknik lider adıyla koşturdu: yarıda kırılan migration hiçbir iz bırakmıyor, `user_version` ilerlemiyor, düzeltilince temiz yükseliyor. `migration_is_idempotent` ve `existing_v1_database_...` de yeşil. |

| **O40** | **Teknik lider yanlış sözleşme kararı verdi — kayda geçiyor** | — | — | — | O36'ya "owner açılırları yeni fiil gerektirmez, `users.list()` kullan" dedim. **Yanlıştı:** `manifest.ts:249` `users.list` → `kind:'http', reason:'spec-8'` ve `SYNCDESKTOP.md:617` `users.*`'ı çevrimiçi-zorunlu ilan ediyor. Oraya bağlansaydı sahip filtresi çevrimdışı **boş kalırdı** — turun çözmek için açıldığı hatanın aynısı, üstelik `tsc` ve `check-data-wiring` yeşil kalacağı için **sessizce**. <br><br>Şerit ölçtü, §8'in talimatı ezdiğini tespit etti, sessizce uygulamak yerine raporladı. Konulan "127 metot" tavanı da hatalıydı: manifest'te dört domain (`contacts`/`companies`/`leads`/`tasks`) **zaten kendi** owner-options fiiline sahip, hepsi aynı `user_list`'e bağlı — yani `deals`'ın kendi fiiline sahip olması desene uyar, ödünç alması anomalidir. Tavan kaldırıldı, `deals.ownerOptions()` eklendi. <br><br>**Ders:** sözleşme kararı verirken "bu fiil zaten var" yetmez — o fiilin masaüstünde **hangi `kind` ile bağlı olduğu** kontrol edilmeli. §8 çevrimiçi-zorunlu bir fiil, çevrimdışı bir ihtiyaca cevap veremez. |

| ~~**O41**~~ | **Kanban kartı rozeti — KAPANDI 2026-08-31** | ✅ | ✅ `crm.ts:237` `boardCard(): WithSyncState<DealCard>`; `DealBoardCard.tsx:59` `<SyncStateBadge compact />`, liste sayfalarıyla **aynı desen** | ✅ Web'de `sync_state` hiç üretilmiyor (`dealsApi.ts` 0, `web.ts` 0) → rozet erken `null` döner; platform dallanması yok. tsc (frontend+desktop), i18n, iki build, `check-data-wiring` yeşil. |
| ~~**O42**~~ | **`dealsShared.ts` / `ticketsShared.ts` inline axios — KAPANDI 2026-09-01, 10/10** | ✅ | ✅ 10 doğrudan `api.get` çağrısının **onu da** sözleşmeye taşındı: `DealsSource` +4 (`tags`, `customFields`, `contactOptions`, `companyOptions`), `TicketsSource` +6 (+`allCompanyOptions`, `userOptions`). Web yolu **birebir** korundu — fetcher'lar taşınmadı, export edilip `platform/web.ts` onlara delege ediyor. Masaüstü yarısı `crm.ts`/`work.ts`'te aynadan okuyor. Manifest **128 → 138** | ✅ `formLookups.test.ts` 13 test (`../http` fırlatacak şekilde mock'lu — ağa kaçış = kırmızı) + negatif kontrol 3 testi kırmızıya çevirdi; `npm run check:data` **138/138 EXIT=0** (teknik lider koştu) | **O40 tuzağı bu turda ikinci kez kuruldu ve bu kez fark edildi:** `manifest.ts` `users.list` → `kind:'http', reason:'spec-8'`; ticket atanan açılırı oraya bağlansa çevrimdışı **sessizce boş** kalırdı. Bunun yerine dört domainin zaten sahip olduğu `user_list` fiili tickets için de açıldı. `contacts.customFields` de ödünç alınamazdı — `entity_type=contacts`'e sabit, deal formuna kişi alanlarını doldururdu ve `tsc` + `check:data` yeşil kalırdı. Aşağısı tarihçedir: `fetchDealTags`, `fetchDealCustomFields`, `fetchDealContactOptions`, `fetchDealCompanyOptionsSearch` ve `useTicketCompanyOptions` doğrudan axios kullanıyor; sözleşmede karşılıkları var (`contacts.tags`, `contacts.customFields`, `contacts.list`, `companies.list`). Deal formu masaüstünde çevrimdışı etiket/özel alan/kişi açılırlarını dolduramıyor — O36'nın panoda çözdüğü sorunun form karşılığı. |

| ~~**O43**~~ | **§7.2/8 birleşik arama — KAPANDI 2026-08-31** | ✅ | ✅ `comms.ts` iki yarı paralel; tekilleştirme `type:id` üzerinden ve **yerel sürüm kazanıyor** (push edilmemiş düzenleme ekranda kalsın); sunucu patlarsa yerel sonuçlar dönüyor, hata yutulmuyor; etiket `CommandPalette.tsx:334` | ✅ 6 test. Manifest'e **`kind: "hybrid"`** eklendi — hem yerel okuma hem `http` **zorunlu**, `mutate` **yasak**. §8 kontrolü bilerek `!== "http"` kaldı: hybrid'in yerel yarısı, sunucunun vermediği bir başarıyı bildirebilir. Negatif kontrol teknik liderce koşuldu: `http.get` kaldırılınca **EXIT=1**. |
| ~~**O44**~~ | **Quick-capture çelişkisi çözüldü — F5'e devredildi, 2026-08-31** | Kullanıcı onayıyla `SYNCDESKTOP.md` §7.2'ye dipnot eklendi (`#k-quickcapture-f5`) + §13 günlük satırı. **F5 madde 3 kazandı:** quick-capture'ın tetikleyicisi global hotkey'dir ve hotkey tartışmasız F5-3'tür; hotkey'siz bir pencere açılış yolu olmayan güdük bir pencere olurdu. **F4 kabulü bu maddeden etkilenmiyor** — "§7.2 tamamı" bundan böyle "quick-capture hariç yedi madde" diye okunur. |

| ~~**O45**~~ | **B1 action beyaz listesi — KAPANDI 2026-08-31** | ✅ sunucu değişti (sözleşme ve istemci doğruydu) | ✅ `MutationApplier.php:473` birleşik `entity.action` anahtarı; `:465` noktalı `action` **açık redde** — tek lehçe, Postel bilinçle reddedildi | ✅ Teknik lider koşumu: **1430 passed** (taban 1427 + 3 yeni). 11 fixture çıplağa çevrildi. **Kırmızı kanıtı:** eski davranışa geçici dönüşte 3 test düştü, md5 ile geri yükleme doğrulandı. |
| ~~**O46**~~ | **B2 otomatik toparlanma — KAPANDI 2026-08-31** | ✅ döngü F4'e alındı (§5.5 tetikleyicisi "open") | ✅ `lib.rs:139` `setup()`'ta başlıyor, handle `AppState.scheduler`'da; `probe_online()` → `load_manifest(true)` — **`force` olmadan** 10 dk'lık önbellek ağa hiç dokunmadan "online" derdi; ramp 1/2/4/8/16, tavan **30 sn** (30 + 4.8 ≪ 60 sn garantisi) | ✅ 4 test, crate **138** (+4). Negatif kontrol kalıcı **503** ile: soket bağlanıyor, sunucu cevap veriyor — naif bir probe'un "online" diyeceği en keskin yanlış-pozitif vaka. Throttle kaynaktan doğrulandı (`throttle:30,1,sync`, ~6 istek/dk). |
| ~~**O47**~~ | **B3 `task.complete` payload'ı — KAPANDI 2026-08-31** | ✅ | ✅ `work.ts:163` → `runAction('task', id, 'complete', { completed: true })`; sunucu kuralı `CompleteTaskRequest.php:26` (`required, boolean`) kaynağından alındı, tahmin edilmedi | ✅ `MutationApplier.php:478` doğruluyor: REST ve offline-action yolu **aynı** FormRequest'i paylaşıyor |
| ~~**O48**~~ | **B4 red gösterimi — KAPANDI 2026-08-31 (iki turda)** | ✅ | ✅ `ConflictInbox.tsx` kayıt adı veya çevrilmiş varlık tipi gösteriyor; `errors.INVALID_MUTATION` dört dilde | ✅ **İlk teslim yarımdı (bulgu B7):** anahtar dört sözlüğe girdi ama `errors.ts` `KNOWN_ERROR_CODES` setine girmedi, `errorMessage()` `errors.unknown`'a düşüyordu — Çakışma Kutusu her sunucu reddini *"An unknown error occurred."* diye gösteriyordu. İkinci senaryo koşumu kasıtlı bir red probe'uyla yakaladı. **Kök neden teknik liderin dosya bölümlemesi:** şeride `desktop.json` + `ConflictInbox.tsx` verildi, `errors.ts` verilmedi. Teknik lider tek satırla düzeltti; simetri denetimi artık **19/19**. |
| **O49** | **B5 — bootstrap sayısı ile aynadaki satır sayısı tutmuyor** | ❌ | ❌ | ❌ | Bootstrap ekranı "Company: 22 records" diyor, `retention_days=30` penceresi sonrası aynada 1 firma kalıyor (sunucuda da son 30 günde güncellenmiş 1 firma var). Gösterim tutarsızlığı, **veri kaybı değil** — ama kullanıcıya yanlış sayı gösteriyor. |

| **O50** | **TS nullability ile sunucu FormRequest nullability'si ayrışıyor** | ⚠️ | kısmen | ❌ | O47 denetiminin asıl bulgusu. Üç fiil üç farklı durumda: <br><br>• **`ticket.assign` DOĞRU** — `AssignTicketRequest.php:34` `present, nullable` ve gerekçesi dosyada yazılı ("ticket havuza geri bırakılır"); `AssignTicketModal` açık bir `UNASSIGNED_VALUE` taşıyor. Bilinçli ürün kararı. <br>• **`task.assign` RİSKLİ** — `AssignTaskRequest.php:24` `required, integer` (nullable **değil**), ama `AssignTaskModal.tsx:34` boş seçimde açıkça `null` gönderiyor (`assignedTo ? Number(assignedTo) : null`). Modal boş seçim üretebiliyorsa atama kaldırma **hem web'de hem masaüstünde** reddedilir. <br>• **`deal.assign` GİZLİ** — `AssignDealRequest.php:24` `required`, TS imzası `number | null`, ama `AssignDealOwnerModal.tsx:33` her zaman `Number(ownerId)` gönderiyor. Bugün ulaşılamaz; imza fazla geniş. <br><br>**Genellenebilir kural:** `DataSource` imzasındaki nullability, karşılık gelen FormRequest'in nullability'siyle **eşleşmeli**. Bu statik olarak kontrol edilebilir — `check-data-wiring` deseninde bir kontrolör konusu. Offline'a özgü değil, paylaşılan web+masaüstü tip gevşekliği. |

| **O51** | **`wire-fixtures/` — RISK-1'in beşincisini engelleyecek mekanizma** | ✅ karar verildi | ❌ | ❌ | **Dört vakanın ortak deseni:** her takım kendi mock'unu test ediyor, iki mock'un aynı bayt dizisini tarif ettiğini hiçbir şey doğrulamıyor. (AUTH-1'de 16 uyuşmazlık · `ApiErrorBody` · SLA kolonlarının `upsert`'te sessizce düşmesi · B1 action lehçesi.) <br><br>**Yapı:** repo kökünde versiyonlanan kanonik JSON seti — `push/` (her op + her beyaz-liste action'ı), `pull/` (entity başına satır), `errors/` (`ApiErrorBody` şekilleri). **Üç tüketici, kopya yasak:** (1) crate `to_wire()` çıktısını fixture'a eşitler · (2) PHPUnit fixture'ı gerçek endpoint'e basıp `applied` bekler · (3) TS composer (vitest) outbox'a giden gövdeyi fixture'a eşitler. <br><br>Kilit özellik: bir taraf wire'ı **tek başına** değiştiremez — fixture'a dokunmadan kendi tarafını değiştirirse kendi testi kırmızı, fixture'ı değiştirirse karşı tarafınki. **O47'yi (crate doğru serileştiriyor, TS boş payload veriyor) yalnız 3. tüketici yakalayabilirdi.** <br><br>**Maliyet:** ilk kurulum 1.5–2.5 gün, CI +<1 dk. Kalıcı: her wire değişikliği üç yerde yeşillenmek zorunda — bu sürtünme kusur değil, mekanizmanın kendisi. <br><br>**Sıra: Dalga 2** — O45'in çıktısını kilitleyeceği için ondan sonra. docs/ENGINEERING-RULES.md'ye eklenecek kural: *"Fixture'ın kapsadığı bir şekil için hiçbir test kendi inline JSON'unu kanıt sayamaz."* |

| **O52** | **`mappers.ts::syncState` export edilmemiş** | — | — | — | `mappers.ts:84` `function syncState(row): SyncState | undefined` — komşuları (`nameRef`:92, `fullNameRef`:98, `titleRef`:104, `fullName`:110) export'lu, bu değil. O41 şeridi `crm.ts`'ten ona erişemedi ve **kendi doğrulayıcısını yazmak yerine** zaten paylaşılan `components/shared/recordSyncState`'i kullandı — doğru karar, ama ayna tarafında aynı ihtiyaç bir daha çıkarsa aynı ikilem tekrarlanır. Tek satırlık `export` + `crm.ts`'in ona yönlendirilmesi; `mappers.ts` sahibi olan bir sonraki tur halledebilir. |

| ~~**O53**~~ | **`comms.test.ts` / `mappers.test.ts` regresyon kapısına bağlı — KAPANDI 2026-09-01, bağımsız denetimle** | ✅ | ✅ `desktop/vitest.config.ts` var, `include: ['src/**/*.test.ts']`; `npm test` kapıda | ✅ teknik lider koştu: **14 dosya / 313 test, hepsi geçti**; `npx vitest list` çıktısında `comms.test.ts` ve `mappers.test.ts` **ikisi de** listede | **Satır 2026-09-01'e kadar KODUN GERİSİNDEYDİ** — vitest bir sonraki turda kurulmuş, defter güncellenmemişti. Aşağısı tarihçedir: İki TS test dosyası (13 test) repoda, ama projede TS test runner`ı (vitest/jest) kurulu değil; şeritler scratchpad`teki bir loader shim`i ile koşturdu. **Yazılmış ama kapıya bağlı olmayan test, bozulduğunda kimseye haber vermez.** `vitest` eklemek `package.json` değişikliği ister — küçük ama bilinçli bir karar. O51 (wire-fixtures) üçüncü tüketicisi olarak zaten bir TS koşucusu gerektirecek; ikisi birlikte yapılmalı. |
| **O54** | **`check-data-wiring` sessiz geçiş açığı kapatıldı — kayda geçiyor** | ✅ | ✅ | ✅ | O43 turunda bulundu: `helperBody()` bilinmeyen bir helper adı için **boş string** dönüyordu, yani `SHARED_HELPERS``ta yanlış yazılmış bir ad üyeyi kendi tek satırıyla sınıflandırtıp **sahte yeşil** üretirdi. Artık `fail()` veriyor. **CRLF olayıyla (O38) aynı sınıf:** kontrolörün kendisi sessizce yanlış cevap veriyordu. Kapanmış madde, tekrar açılmasın diye kayıtta. |

| ~~**O55**~~ | **Hata kodu simetri kontrolörü — KAPANDI 2026-08-31** | ✅ | ✅ `desktop/scripts/check-error-codes.mjs`; `KNOWN_ERROR_CODES` bulunamazsa **throw** (sessiz boş liste yok), blok içi yorum satırları regex'ten önce eleniyor (yorumlardaki apostrof yanlış eşleşme üretebilirdi), hariç tutma **yapısal kuralla** (SCREAMING_SNAKE deseni), açık listeyle değil | ✅ Bugün **19/19**. Teknik lider iki yönü de ayrı koşturdu: kodda-var/sözlükte-yok → EXIT=1 doğru mesajla; sözlükte-var/kodda-yok (B7'nin sınıfı) → EXIT=1; geri alınca 0; kalıntı 0. **Şerit teknik liderin negatif kontrol tasarımındaki hatayı yakaladı:** verilen iki eylem aynı dalı tetikliyordu, ters yön kanıtlanmamış olacaktı. |
| **O56** | **B6 — reddedilen action`ın iyimser yerel yazması geri alınmıyor** | ❌ | ❌ | ❌ | İkinci koşum buldu: ilk koşumun reddedilen `deal.move``ları `TakeServer` + tam senkron sonrası bile aynada duruyordu (ayna 26@2/32@3/48@5, sunucu 1/2/3). `theirs=null` olduğu için yazacak satır yok; sunucu değişmediği için pull da göndermiyor. Yani reddedilen bir action, yerelde **kalıcı yanlış durum** bırakabiliyor. O45 sonrası red nadir ama sıfır değil (izin hataları, doğrulama). |
| ~~**O57**~~ | **B8 Çakışma Kutusu tazeleme — KAPANDI 2026-08-31** | ✅ | ✅ `ConflictInbox.tsx:161` liste artık `useEngineStatus().conflicts`'e bağlı | ✅ **Kök neden:** liste `useEffect(..., [load])` ile yükleniyordu ve `load` yalnız `t`'ye bağlı olduğu için **fiilen mount-anı** çalışıyordu — hiçbir canlı sinyale abone değildi; rozet ise `StatusChanged` akışındaydı. Rozeti besleyen **aynı** hook'a bağlandı, yeni mekanizma kurulmadı. |
| ~~**O58**~~ | **G1 bayat yorum — KAPANDI 2026-08-31** | `commands/sync.rs` `sync_now` yorumu güncel duruma uyduruldu (döngü `setup()`'ta başlıyor, offline'da probe var, `sync_now` hâlâ elle tetiklenen tek tur). Dosyanın kalanı tarandı, başka bayat ifade yok. |
| **O59** | **G2 — hiç senkronlanmamış yerel kaydı silmek `UNRESOLVED_REFERENCE` üretiyor** | ❌ | ❌ | ❌ | İkinci koşum gözlemi. Sunucunun hiç duymadığı bir kaydın silinmesi, çözülecek referansı olmayan bir mutasyon üretiyor. Beklenen davranış outbox`tan sessizce düşürmek olmalı (create + delete çifti coalesce edilebilir). |

| ~~**O60**~~ | **`onConflictAdded` — KAPANDI 2026-09-01, KALDIRILARAK** | ✅ kaldır | ✅ `bridge/events.ts` handler tipi ve çağrısı silindi; `conflict_added` case'i artık **açıkça no-op** ve nedenini yorumda taşıyor. Rust `EngineEvent::ConflictAdded` yayınına dokunulmadı (sözleşmenin parçası) | ✅ `bridge/events.test.ts` 5 test: no-op davranışı, hiçbir handler tetiklenmemesi, atmama, **negatif kontrol** (`status_changed` gerçekten tetikliyor) ve `EngineEventHandlers` şekil kilidi. Kilit gerçek: alan geri eklenince `tsc` **TS2344** ile kırılıyor | **Gerekçe (ölçümle):** `sync/mod.rs:544-546` her yeni çakışma için `ConflictAdded` yayınlıyor, ama aynı round'un dönüşünde `sync_now` (`:464`) koşulsuz `refresh_status()` çağırıyor — yani her `conflict_added`'ı, güncel sayıyı taşıyan bir `status_changed` **zaten** izliyor. `ConflictInbox.tsx:131-161` ona bağlı. Bağlamak ikinci bir tetikleyici (ileride çift toast) demekti, bilgi eklemiyordu. <br><br>**Yan etki — kontrolör düzeltmesi:** yeni test dosyası `check:realtime`'ı kırdı (`invalidateQueries` yalnız `bridge/events.ts`'te olabilir kuralı, mock'u üretim kodu sandı). `check-realtime-wiring.mjs`'e **dar** muafiyet eklendi: yalnız `bridge/events.test.ts`, genel `*.test.ts` **değil**. Negatif kontrol koşuldu — başka bir test dosyasına konan `invalidateQueries` hâlâ **EXIT=1** (O54'ün "kontrolörü zayıflatma" dersi). O57 turunda yol boyunda bulundu: `bridge/events.ts:154,181` `onConflictAdded` handler`ını tanımlıyor ve çağırıyor, ama `main.desktop.tsx` `subscribeToEngineEvents``a yalnız `onStatusChanged` ve `onAuthLost` veriyor (grep: `onConflictAdded` orada **0** eşleşme). Rust tarafı `EngineEvent::ConflictAdded` yayınlıyor, istemci onu **sessizce atıyor**. <br><br>Bugün zararsız — `status.conflicts` aynı bilgiyi taşıyor ve O57 listeyi ona bağladı. Ama tanımlanmış, çağrılan ve hiçbir yere ulaşmayan bir olay yolu, bir sonraki okuyucuya "bu bağlı" izlenimi verir. Ya bağlanmalı ya kaldırılmalı. |

| ~~**O61**~~ | **Bildirim ham anahtar hatası — KAPANDI 2026-09-01 (iki turda)** | ✅ | ✅ **Mekanizma:** `features/notifications/notificationText.ts` — `title_key`/`body_key` + `params` i18next'ten çözülüyor, öncelik tersine döndü, çözülemeyen anahtar ham basılmıyor (`desktop:entities.notification`'a düşüyor). **İçerik:** 12 tip × 4 dil = 144 anahtar `backend/lang/*/notifications.php`'den taşındı, `:param` → `{{param}}` mekanik dönüşümle. | ✅ 4 mapper testi + **yeni kontrolör** `i18n:notifications`: iki yönlü anahtar karşılaştırması **artı parametre adı** karşılaştırması. Teknik lider negatif kontrolü koşturdu: `{{deal_title}}` → `{{dealTitle}}` **EXIT=1** ve iki yönü birden raporluyor. <br><br>**Teknik lider'in dispatch varsayımı yanlıştı** ("web zaten çözüyor, yardımcıyı paylaş") — web hiç client-side çözmüyor, sunucu `NotificationResource.php:48`'de çözüp gönderiyor. Şerit uydurmak yerine ölçtü ve düzeltti. |
| ~~**O62**~~ | **Windows badge — KAPANDI 2026-09-01** | ✅ | ✅ `set_badge` iki cfg'li `apply_badge`'e bölündü: Windows `set_overlay_icon`, diğerleri `set_badge_count`. 10 PNG (`1..9`, `9-plus`) `include_bytes!` ile **gömülü** — bundle resource değil, çalışma zamanında kaybolamaz. Üretimleri bağımlılıksız (node:zlib + elle CRC-32 + 3×5 bitmap glyph). | ✅ 3 test: sayı→overlay eşlemesi, 10+/`i64::MAX` taşması, ve on asset'in **gerçekten çözülen, 32×32, birbirinden farklı piksel** üreten PNG'ler olduğu (kesik `include_bytes!` yakalanır). ⚠️ Görev çubuğunda **göz ile doğrulanmadı** — F5 kabulü için elle bir tur gerekiyor. || **O63** | 🟠 **Bildirim tıklaması → `syncra://` bu eklentiyle yapılamıyor** | — | ❌ | ❌ | `tauri-plugin-notification` masaüstü yolu toast'ı `notify-rust``a verip dönüyor; **tıklama callback'i yok** (`desktop.rs:179-222`). §6.4 *"tıklama → `syncra://<entity>/<id>` yönlendirme"* diyor. **F5-4 (deep link) planı buna göre kurulmalı:** deep link'in karşı ucu var, bildirimin tıklama ucu yok. |
| ~~**O64**~~ | **`RunEvent::Exit` — KAPANDI 2026-09-01, ÖLÇÜMLE** | ✅ | ✅ Tray Quit → `app.exit(0)` → `RunEvent::Exit` → `scheduler.stop()` **sonra** `engine.shutdown()`. Sıra motorun kendi dokümanından: önce durdur ki `wal_checkpoint` hâlâ yazan bir turla yarışmasın. | ✅ **Şerit uygulamayı gerçekten çalıştırdı, teknik lider diskte doğruladı:** `.window-state.json` **211 B** (bu makinede ilk kez oluştu) · `syncra.db-wal` **4.239.512 B → yok** · `-shm` **yok**. Üç sonucun üçü de ölçüldü. |
| ~~**O65**~~ | **`StateFlags` `VISIBLE` tuzağı — KAPANDI 2026-09-01** | ✅ | ✅ `lib.rs:83` `StateFlags::all() & !StateFlags::VISIBLE` | ✅ **Kaynak-assert + davranışsal ölçüm birlikte.** Yazılan dosyada `"visible": true` — ve ölçümde pencere çıkışta **gizliydi** (tray'e inmişti). Bayrak dursaydı `false` yazılacak ve uygulama sonraki açılışta **hiç görünmeyecekti**. Kilit: `tray::tests::window_state_does_not_persist_visibility` + `the_exit_callback_is_wired`. |
| ~~**O66**~~ | **Autostart durumu açılışta okunuyor — KAPANDI 2026-09-01, bağımsız denetimle** | ✅ | ✅ **beşinci komut yazılmış**: `commands/os.rs:362` `get_autostart`, `lib.rs:171` `generate_handler!`'da kayıtlı, TS `desktop/src/ui/commands.ts:206` `readAutostart`, ekran açılışta okuyor: `DesktopPreferences.tsx:74` `setAutostartState(await readAutostart())` | ✅ `os.rs:662` `autostart_is_opt_in` + `os.rs:673` idempotens testi | **Satır 2026-09-01'e kadar KODUN GERİSİNDEYDİ.** Çözüm defterin andığı `@tauri-apps/plugin-autostart` npm paketiyle değil, önerilen beşinci Rust komutuyla yapılmış. Aşağısı tarihçedir: §6.2 yalnız dört `os::*` komutu listeliyor, **okuma komutu yok**; `@tauri-apps/plugin-autostart` npm paketi de `desktop/package.json``da değil. `set_autostart` geri-okunan gerçek değeri döndürüyor ama bu ancak **yazdıktan sonra** işe yarar — ayar ekranı açılışta mevcut durumu gösteremez. Beşinci bir komut mu, npm paketi mi: **kullanıcı kararı** (§0.5 gereği şerit kendi eklemedi). |

| ~~**O67**~~ | **GİZLİLİK: wipe cache blob'larını da siliyor — KAPANDI 2026-09-01** | ✅ | ✅ `sync/mod.rs:183` `wipe_local()` — `SELECT DISTINCT path` → `remove_cached_blob` → `db::wipe`; üç çağrı noktası da dönüştü (`:311` farklı-kullanıcı login, `:397` logout, `:1380` A25 403) | ✅ Üç mevcut wipe testi blob assert'iyle genişletildi. **Negatif kontrol:** `wipe_local` `db::wipe`'a geri indirgenince **üçü de kırmızı** — kendi panik mesajlarıyla (*"O67: a different-user login must remove the previous user's cached blobs"*). Artık A kullanıcısının teklif PDF'leri B login olduğunda diskte kalmıyor. |
| ~~**O68**~~ | **Gerçek katalog + gerçek mapper testi — KAPANDI 2026-09-01** | ✅ | ✅ `notification-catalog.test.ts` — **gerçek** dört `notifications.json` gerçek i18next singleton'ına yükleniyor | ✅ **145 test** (4 dil × 36 çift), katalog üzerinde **döngü** — 13. tip eklendiğinde dosyaya dokunmadan kapsanıyor. `params` fixture'ları 12 bildirim sınıfından satır referanslı transkribe edildi. **Teknik lider negatif kontrolü koşturdu:** Almanca çeviri ham anahtarla değiştirilince tam bir test kırmızı, mesajı isabetli (*"resolved to the raw key, not a translation"*); geri alınca 145 yeşil. |
| ~~**O69**~~ | **Cache tavanı anlık uygulanıyor — KAPANDI 2026-09-01** | ✅ | ✅ `record_cached_file` sonunda `trim_cached_files` (`retention.rs:108`) — §5.6/3'ün zaten yazılı invariant'ı, ekleme değil yerine getirme | ✅ Üç mevcut test yeni invariant'a göre yeniden yazıldı (teknik lider, §4 sapması — aşağıya bakın). `THIRD_CEILING` gerektirdi: iki 60 MB kayıtta ikincisi zaten tahliye ediyor, touch'ın söz hakkı kalmıyor. Her iki tavan testine `run_retention → removed == 0` assert'i eklendi. |

| ~~**O70**~~ | **§6.4 "Pencere kapatma → tray'e **(ayar)**" — KAPANDI 2026-09-01, bağımsız denetimle** | ✅ | ✅ zincirin dördü de yerinde: crate ayarı `config.rs:82-83` `close_to_tray` (`serde(default)`, varsayılan true) · motor API `sync/mod.rs:1000` · pencere handler'ı okuyor `src-tauri/src/lib.rs:264,288` · UI toggle `DesktopPreferences.tsx:211-216` | ✅ `config.rs:163` serde default testi + `tests/retention_storage.rs:179-232` kalıcılık testi | **Satır 2026-09-01'e kadar KODUN GERİSİNDEYDİ.** Hazır bekleyen `desktop.window.closeToTray.*` anahtarları da artık canlı (`tr/desktop.json:255`). Aşağısı tarihçedir: Davranış sabit D-8; kullanıcı seçemiyor. `DesktopSettings``e alan (motor API'si) + `desktop/src` toggle'ı gerekiyor. **i18n metni `desktop.window.closeToTray.*` dört dilde zaten hazır bekliyor** — F5-1'in kapsamı dışındaydı. |
| ~~**O71**~~ | **Duraklatılmış durum UI'a ulaştı — KAPANDI 2026-09-01** | ✅ motora alan **eklenmedi** (danışman kararı: motorun dolduramayacağı alanı motora taşımak iki gerçek kaynağı doğurur) | ✅ Kabuk düzeyi zenginleştirme: `StatusWithPause` (`#[serde(flatten)]`), kaynak scheduler slotu. `enrich()` motorun kendi `Serialize`'ından geçiyor, alan adlarını elle kopyalamıyor. `toggle_pause` sonrası olay basılıyor — motor pause için hiç olay basmadığından bu olmadan UI 60 sn (çevrimdışıysa hiç) haberdar olmazdı. | ✅ src-tauri **82** (+9); 6 olay testi. Kilit olan **`other_variants_pass_through_unchanged`**: zenginleştirme diğer beş varyantı byte-byte bozmuyor. Web'de alan hiç dolmuyor, platform dallanması yok. |
| ~~**O72**~~ | **Webview dil override'ı tray'e yansıyor — KAPANDI 2026-09-01, bağımsız denetimle** | ✅ | ✅ defterin önerdiği çözüm **aynen** uygulanmış: komut `commands/os.rs:445` `set_tray_language` (`lib.rs:175` kayıtlı) · TS `commands.ts:256` · abonelik `main.desktop.tsx:105` `i18n.on('languageChanged', pushTrayLanguage)` + açılışta ilk itiş `:126` · Rust önceliği **override > session > OS** `tray.rs:219-251` | ✅ `tray.rs:848-857` — override kazanıyor, geçersiz override session'a düşüyor | **Satır 2026-09-01'e kadar KODUN GERİSİNDEYDİ.** Aşağısı tarihçedir: Tray etiketleri oturumun hesap dilinden geliyor ve bu **ölçüldü**: OS locale `tr-TR` iken menü İngilizce çıktı (hesap dili `en`) — yani ne OS'tan ne sabit metinden. Ama kullanıcı webview'de dili elle değiştirirse (`localStorage`) tray hesap dilinde kalır; Rust `localStorage``ı okuyamaz (WebView2 profili içinde). Çözüm: `set_tray_language` komutu + `languageChanged` aboneliği. |
| ~~**O73**~~ | **`i18n:dead-keys` Rust tüketicisi — KAPANDI 2026-09-01** | ✅ | ✅ Script'e Rust taraması **eklenmedi** (serde alan adları literal anahtar değil, bespoke ayrıştırıcı aşırı mühendislik). Yerine prefix allowlist. | ✅ **Allowlist kör nokta açmıyor, sahipliği devrediyor:** bir `tray.*` anahtarı silinirse `tray.rs::tests::every_language_parses` kırmızı verir. Bu ayrım yorumda belgelendi. |

| ~~**O74**~~ | **`settings()` artık kalıcı satırı okuyor — KAPANDI 2026-09-01** | ✅ | ✅ İki kaynak ayrıldı: `retention_days`/`max_db_size_mb`/`max_outbox` → **`cfg`** (motorun fiilen uyguladığı clamp'li değer; "raporlanan ayar" ile "uygulanan ayar" sessizce ayrışmasın diye), `clipboard_capture`/`close_to_tray` → **kalıcı `SETTING_PREFERENCES` satırı**. Satır yok / motor kapalı / JSON bozuk → üçü de `Default::default()`. Yalan söyleyen doc düzeltildi. | ✅ **Negatif kontrol:** eski hardcode'a geri dönülünce **iki test kırmızı** (*"clipboard_capture must survive a restart"*, *"a write to close_to_tray must be visible to the very next read"*). 4 yeni test: yaz-sonra-oku, satır yok, bozuk JSON, restart kalıcılığı. |
| **O75** | **§4 SAPMASI — teknik lider crate test kodu yazdı** | — | — | — | Bir şerit işi yarıda bırakıp ağacı kırık bıraktı (crate 155 → 129). Teknik lider üç testi yeni invariant`a göre yeniden yazdı; normalde docs/ENGINEERING-RULES.md §4 gereği onun işi değil. **Gerekçe:** kırmızı ağaç §0.4 kapısını ve sonraki her şeridin doğrulama zeminini bloke eder — kırmızı üstüne açılan şerit kendi kırmızısını ayırt edemez. **Bedel ödendi:** negatif kontrol koşuldu (LRU testinde touch devre dışı → kırmızı → geri al). Danışman bağımsız okuyup aritmetiği doğruladı ve *"testler amaç kaybetmemiş, güçlenmiş"* dedi. <br><br>**Emsal değil, istisna.** Kalıcı kural: kırık-ağaç kurtarma protokolü — (1) kapsamı `git diff` + test çıktısıyla ölç, (2) üretim değişikliği **tam ve karara dayanıyorsa** testler hizalanabilir, her biri için negatif kontrol **zorunlu**, test silmek yasak, (3) üretim yarımsa geri alma seçeneği tam dosya listesiyle kullanıcıya sunulur, (4) müdahale faz raporunda beyan edilir. |

| ~~**D4**~~ | **`attachments:prune-orphans` zamanlandı — KULLANICI ONAYI ALINDI 2026-09-01** | ✅ | ✅ `routes/console.php` → `Schedule::command('attachments:prune-orphans --force')->dailyAt('03:47')`; komutun **kendi** 24 saatlik eşiği ezilmedi | ✅ `ConsoleScheduleTest` — kayıt sessizce düşerse test kırmızı. <br><br>**§8 ONAY KAYDI.** Kullanıcıya sunulan kanıt: salt okunur sayım (SELECT, DELETE yok) → **7 satır silinecek**, en eski 2026-07-20, **en yeni 2026-08-24 (8 gün)**, eşik altında korunacak 0. Komut hem DB satırını hem **disk dosyasını** kalıcı siliyor (`PruneOrphanAttachments.php:101-102`, soft-delete yok). Riski düşüren yapı: `StoreMessageRequest.php:91-97` eki **gönderim anında** doğruluyor — budanmış bir ek sarkık referans değil, temiz doğrulama hatası üretir. <br><br>**Kalan şart (prosedürel):** ilk zamanlanmış koşumdan sonra sayım bir kez daha alınıp faz raporuna girsin. |

| **O76** | **Kapıya bağlanan test, kapının koştuğu ORTAMDA yeşil olmalı** | ✅ ders | ✅ düzeltildi | ✅ | CI `frontend` işi kırmızı verdi, sebep teknik liderin kapıya eklediği `npm test`'ti. Kök neden **locale değil, test dosyaları arası bulaşma**: `mappers.test.ts` `notifications.task_assigned.body_with_due` anahtarını seed ediyordu ama o anahtar **dört gerçek katalogda da var** — yani gönderilen çeviriyi eziyordu. Yerelde diğer diller vitest ortamında yüklü olmadığı için hep `tr``ye düşüyordu; CI'da `notification-catalog.test.ts` dördünü de singleton'a yükleyip dili değiştirdiği için gerçek İngilizce karşılık çözülüyordu. <br><br>**İki katmanlı düzeltme:** dil pinlendi + test anahtarı çakışamaz hâle getirildi (`body_seeded_by_this_test_only`). Tek başına pin kırılgandı — `en` pinleyip yerelde koşturunca **yine geçiyordu**, yani düzeltmenin sonucu belirlemediği görüldü. <br><br>**Kural:** bir testi kapıya bağlamadan önce kapının ortamında koştur; `--no-file-parallelism` paylaşılan singleton bulaşmasını yerelde ortaya çıkarır. |

| ~~**A3**~~ | **`screenshots` → `xcap` — KAPANDI 2026-09-01, ÖLÇÜTLE** | ✅ | ✅ `xcap 0.9.8`; `capture_region` `u32` aldığı için `i32→u32` `try_from` ile, negatif offset **açık `VALIDATION_ERROR`** (wrap-around yerine). `xcap`'in `image` feature'ı **açılmadı** (o `image/default` demek, tüm decoder setini geri getirirdi). | ✅ **Ölçüt karşılandı, teknik lider doğruladı:** `Cargo.lock` **706 → 703**, `cargo tree -d`'de **hiç `image` kopyası yok** (önce 0.24 + 0.25), Windows gerçek bağımlılık grafiği **364 → 339**. `#[ignore]`'lı gerçek yakalama testi elle koşuldu, geçti. <br><br>⚠️ **İki F7 riski doğdu:** (1) `xcap` **edition 2024** → CI matrisinde Rust **≥1.85** pinlenmeli; (2) **Linux başlık seti genişledi** (`xcb`, `zbus`, `pipewire`, `drm`/`gbm`, `clang`) — geçişin gerekçelerinden biri Linux riskiydi ve o taraf **kötüleşti**. |
| ~~**D1**~~ | **`os::get_autostart` — KAPANDI 2026-09-01** | ✅ | ✅ Dört kaynağa kayıtlı; `set_autostart`'ın iki okuması da ortak `read_autostart()` yardımcısını kullanıyor (aynı hata kodu/mesajı garantisi). §6.2'ye teknik lider ekledi. | ✅ `autostart_is_opt_in` hâlâ yeşil. npm paketi reddi kod yorumunda gerekçeli. |

| **O77** | **`HOTKEY_CONFLICT` ayrı hata kodu yok** | ⚠️ | — | — | `register_hotkey` çakışmada `OS_ERROR` dönüyor; ayrım **ekran seviyesinde** yapılıyor (`desktop.preferences.hotkey.conflict`). Bugün güvenli çünkü komutun tek çağıranı tercihler ekranı. Ama bu **bağlamdan çıkarım**, koddan değil — WIRE-1'in `serverAuthCode()` regex'ini silme gerekçesinin aynısı. İkinci bir çağıran gelirse genel bir OS hatası görür. Eklenirse `check:errors` gereği aynı anda dört sözlüğe + `KNOWN_ERROR_CODES`'a girmeli. |
| **O78** | **F5 Windows'ta ELLE doğrulanmadı** | — | ✅ kod | ⚠️ birim test | §10 F5 kabulü *"Windows'ta her madde doğrulanmış"* diyor. Şu ana kadar **yalnız F5-1 (tray)** gerçek uygulamada ölçüldü. Göz ile doğrulanmamışlar: tray tek-tık · hotkey çakışması · quick-capture penceresinin gerçek boyutu/frameless görünümü · `syncra://` linkinin OS'tan gelişi · pano bildirimi · **badge overlay'in görev çubuğunda görünmesi**. Birim testler + derleme ile sınırlı. **F5 kapanışı bu tura bağlı.** |
| ~~**O79**~~ | **"7 komut TS'ten çağrılmıyor" — ARTIK YANLIŞ, KAPANDI 2026-09-01** | ✅ | ✅ yedisi de bağlanmış (`attach_from_paths`, `cache_quote_pdf`, `open_cached`, `screenshot_to_ticket`, `notify`, `set_autostart`, `set_badge`) | ✅ teknik lider koştu: `npm run check:commands` → **EXIT 0**, 30 komut / 30 kayıtlı / 30 TS'ten invoke, **dead commands: 0**, unresolved dynamic: 0 | **Satır 2026-09-01'e kadar KODUN GERİSİNDEYDİ** — F5 bağlamaları yaptı, defter güncellenmedi. Aşağısı tarihçedir: `check:commands` uyarısı: `attach_from_paths`, `cache_quote_pdf`, `open_cached`, `screenshot_to_ticket`, `notify`, `set_autostart`, `set_badge`. Rust yarısı hazır, UI bağlaması yok — sürükle-bırak olay dinleyicisi, ekran yakalama tetikleyicisi, bildirim üreticisi, autostart/badge ayar bağlaması. Danışman kararı gereği sarmalayıcı **tüketicisiyle birlikte** gelir; bu sayının F5 kapanışında **sıfıra inmesi** ayrıca bir kabul göstergesi. |

| ~~**O80**~~ | **`mapNotification` ayna `data` kolonunu ayrıştırmıyordu — KAPANDI 2026-09-01** | ✅ | ✅ `mappers.ts:744` yeni `notificationData()`; string ise `JSON.parse`, nesne ise aynen, bozuk/boş/dizi/skaler → `{}` **istisna fırlatmadan** (tek bozuk satır listeyi düşürmemeli). Desen `mapSavedView`'ın `query_json` işleyişinden alındı. | ✅ `mappers.test.ts` +4, hepsi **gerçek ayna şekliyle** (`data` string, `created_at` boşluklu). **Negatif kontrol koşuldu:** helper eski tek satıra döndürülünce 2 test kırmızı (`Expected: 'Size bir fırsat atandı' / Received: ''`). | **Kök neden zincirle doğrulandı:** `0001_init.sql:359` `data TEXT` → `SyncPullService::renderNotificationText()` `json_encode` → `db/schema.rs:162` `Entity::Notification = NO_EMBEDDED`, yani `row_to_json()` bu kolonu geri parse etmiyor → webview'e **string** geliyor, `typeof === 'object'` false, `data = {}`. **Kullanıcıya görünen semptom: bildirim listesi tamamen boştu** (5 satır, yalnız göreli zaman). Toast'ların düşmesi bunun yan etkisiydi. |
| ~~**O81**~~ | **Ayna damgası yerel saat okunuyordu — KAPANDI 2026-09-01** | ✅ | ✅ Yeni **paylaşılan** modül `platform/data/timestamps.ts::parseMirrorTimestamp()`. Naif `YYYY-MM-DD[ T]HH:MM:SS[.sss]` → `T…Z`; `Z`/offset taşıyan **ve yalnız-tarih** değerler dokunulmadan geçiyor (regex `[ T]HH:MM:SS` kuyruğu şart koşuyor). **Üç tüketici:** `bridge/notifications.ts::takeUnshown`, `ui/format.ts::formatDateTime`, `mappers.ts::isPast`. | ✅ +22 test (`timestamps` 9, `format` 7, `mappers` 6), üçü de **TZ `Asia/Istanbul`'a sabitlenmiş** — UTC makinede bozuk ve doğru okuma aynı sayıyı verir, test hiçbir şey kanıtlamaz; ayrıca host'un gerçekten UTC+3 olduğunu doğrulayan muhafız testleri var. **Teknik lider negatif kontrolü koşturdu:** paylaşılan yardımcının gövdesi `Date.parse(value)`'ya indirgenince **14 test kırmızı, dört dosyada birden** (`timestamps` 4 · `notifications` 7 · `format` 2 · `mappers` 1) — üç tüketici de düşüyor. Geri alınca 260/260. | **Kök neden:** `SyncPullService::fetchRows()` mirror tablolarını `DB::table()` ile, Eloquent'siz okuyor → MySQL `DATETIME` telde kendi metin biçiminde (`2026-09-01 11:07:36`, boşluk, ek yok). **Üretim verisinden doğrulandı** (teknik lider, `syncra_crm.notifications`). Instant UTC (`APP_TIMEZONE=UTC`) ama **string bunu söylemiyor**, ECMA-262 eksiz biçimi **yerel** sayar. UTC+3'te her satır `createdMs < sinceMs` ile eleniyordu; **negatif ofsetli makinede ters yönde çalışır ve birikmiş tüm listeyi aynı anda toast'lar** — priming'in önlemek için var olduğu senaryonun ta kendisi.  <br><br>**DÜZELTME İLK SEFERİNDE YANLIŞ YOLA BAĞLANDI — kayda geçiyor.** İlk tur `desktop/src/ui/format.ts::formatDateTime`'ı düzeltti ve hem docblock hem teknik liderin dispatch'i onu *"bildirim listesinin damgalarını basan fonksiyon"* diye tarif etti. **İkisi de yanlıştı:** grep ile doğrulandı, `formatDateTime`'ın tam **iki** tüketicisi var — `ConflictInbox.tsx:326` ve `DevicesPanel.tsx:114`, ikisi de panel. Bildirim listesi `frontend/src/features/notifications/components/notificationMeta.ts::formatRelativeTime`'ı çağırıyor (`NotificationList.tsx:95`, `NotificationsPage.tsx:166`) ve o dokunulmamıştı. <br><br>**Birim testleri yeşildi ve hata yaşıyordu.** Çalışan uygulamada ölçüldü: 3 dk 25 sn önce yazılmış satır ekranda **"3 hours ago"**. İkinci tur ayrıştırıcıyı `frontend/src/lib/mirrorTime.ts`'e taşıdı (`frontend/src` `desktop/src`'ten import **edemez** — alias tek yönlü, `vite.desktop.config.ts:28`), `desktop/src/platform/data/timestamps.ts` tek satır re-export'a döndü, ve üç gerçek yol bağlandı: `formatRelativeTime`, `lib/datetime.ts::toDate` (dolayısıyla `notificationText.ts:150`'nin bildirim parametreleri), `ui/format.ts`. <br><br>**Web regresyonu yapısal olarak dışlandı:** `NotificationResource.php:61` → `toIso8601String()`, yani web değerleri **daima ofset taşır** ve pass-through dalına düşer; üç test bunu düzeltme öncesiyle bit-bit eşitlik olarak sabitliyor. **Teknik lider negatif kontrolü koşturdu:** paylaşılan gövde `Date.parse(value)`'ya indirgenince **20 test kırmızı, beş dosyada** — aralarında ekranda ölçülen semptomun birebir testi (*"reads a mirror stamp written three minutes ago as MINUTES, not hours"*). Geri alınca 278/278. <br><br>**Ders:** yanlış docblock, düzeltmenin kapsandığı sanısını yarattı ve bir tam tur maliyet çıkardı. Belge düzeltildi ve iki dosyaya açık SCOPE bloğu yazıldı. |
| **O82** | **Fikstürler "gerçek şekil" yerine "rahat şekil" kullanıyor** | ✅ | — | — | O80 ve O81'in **tek ortak nedeni**: birim testleri `data`'yı **nesne**, `created_at`'i **ISO+Z** veriyordu; ayna ikisini de öyle saklamıyor. İki üretim hatası da bu boşluktan geçti ve **hiçbir log izi bırakmadı**. Aynı sınıfın taraması yapıldı — `tags`/`custom_fields`/`items` crate tarafında `embedded` olduğu için sağlam, `saved_views.query_json` zaten `JSON.parse` ediyor, `settings.value` skaler; **`notifications.data` tek kırık örnekti.** <br><br>**Öneri (uygulanmadı):** `wire-fixtures` konformans paketine "ayna şekli" boyutu eklemek — fikstürler bugün rahat şekli taşıyor, üç tüketici de aynı rahat şekle bakıyor, yani mekanizma bu sınıfa karşı **kör**. RISK-1'in aynası: bu sefer sunucu-istemci arasında değil, **crate ile webview arasında** simetrik mock. |

| ~~**O83**~~ | **Ayna iki damga biçimi saklıyordu — KAPANDI 2026-09-01** | ✅ | ✅ **Seçenek (a):** crate sunucu damgalarını **upsert sınırında** kanonikleştiriyor. `db/mod.rs` üç fonksiyon: `is_timestamp_column` (kural `*_at` son eki), `normalize_timestamp` (naif → `YYYY-MM-DDTHH:MM:SS.mmmZ`; ofset/`Z` taşıyan ve yalnız-tarih değerlere **dokunmuyor**), `json_to_sql_for_column`. Hedef biçim `.000Z`, çıplak `Z` **değil** — yerel satırlar `now_iso()` ile `.mmmZ` yazıyor, aynı saniyedeki `...27Z` ile `...27.000Z` string düzeyinde eşit sıralanmazdı. <br><br>**Kolon kuralı şemadan çıkarıldı, tahmin edilmedi:** 111 datetime kolonunun **tamamı** `_at` ile bitiyor, yalnız-tarih kolonlarının (`expected_close_date`, `valid_until`, `rate_date`, `exchange_rate_date`) **hiçbiri** bitmiyor — son ek tam olarak iki küme arasındaki sınır. <br><br>**İki bağlama noktası tarandı ve ikisi de bağlandı:** `upsert.rs::upsert_row` (bootstrap + delta + `download_archive`, dördü de `pull_until_drained` üzerinden) ve `sync/mod.rs::apply_server_row` (çakışma çözümü — `pending_shadows.row_json` geri-uygulama yolu da buradan geçiyor). Kapsam dışı bırakılanlar gerekçeleriyle raporlandı: `local.rs` payload'u (UI, sunucu satırı değil), `now_iso()` yazıcıları (zaten kanonik), `push.rs::settle` (damga yazmıyor). <br><br>**Migration `0003_normalize_timestamps.sql`** — **86 UPDATE / 22 tablo**. 111 kolondan 22 × `local_updated_at` (hiç sunucu değeri taşımadı) ve 3 motor-yerel tablo kolonu **kasten** dışarıda. Yeniden yazım `replace(x,' ','T')` değil **sabit ofsetli dilimleme** — `LIKE`'ın `_` jokeri boşluk da eşleştirdiği için `replace` teorik olarak ikinci bir boşluğu da bozabilirdi. | ✅ crate **160 → 169** (+9: 4 birim, 5 entegrasyon). **Teknik lider negatif kontrolü iki koldan doğruladı.** <br><br>**(a) Runtime normalizasyonu kapatıldı → 5 entegrasyon + 1 birim testi kırmızı.** En anlamlısı defterdeki senaryonun birebir kanıtı: *"newest first means 23:59 above 08:00 — a dialect must never outrank the clock; left: [Offline 08:00, Server 23:59]"*. Keyset cursor da düştü: çevrimdışı 09:30 mesajı sayfadan **eleniyordu**. <br>**(b) Migration zincirden çıkarıldı → yükseltme testi kırmızı**, yani mevcut aynaların gerçekten düzeldiği ayrıca kanıtlandı. | **`overdue` hatası ölçümle doğrulandı:** bugün **17:32**'de vadesi dolacak bir **sunucu** görevi, saat 11:05'te `overdue` listesinde çıkıyordu — `due_at < strftime('%Y-%m-%dT%H:%M:%SZ')` T-biçimli "şimdi" ile boşluklu `due_at`'i karşılaştırdığı ve `' ' < 'T'` olduğu için. Test "dün" görevini pozitif kontrol olarak taşıyor, boş dönen sorgu geçemiyor. |

| **O84** | ⚠️ **Bayat `queue:work` — kuyruktan yazılan bildirimler `sync_version = 0` alıyor** | ✅ | — | — | **Kod hatası değil, ortam durumu — ama "bildirim gelmiyor" şikâyetinin gerçek sebebi bu olabilir, o yüzden kayıtta.** `SyncVersionObserver`'ın `DatabaseNotification`'a bağlandığı satır (`AppServiceProvider.php:109`) commit **`c5b2b79`, 2026-08-31 10:01:46** ile geldi. Çalışan `php artisan queue:work` (pid 3028) ise **2026-08-30 19:23:03**'te başlatılmış — o koddan **15 saat önce**. `queue:work` uzun ömürlüdür ve kodu yeniden yüklemez. <br><br>Sonuç: kuyruktan yazılan her bildirim satırı `sync_version = 0` alıyor; delta pull `WHERE sync_version > cursor` olduğu için o satırı **hiçbir istemci asla görmez**. Ölçülen kanıt: satır `29d6beca-8039-49b0-8d29-3c4ffa3871e6` (`sync_version = 0`, aynaya hiç inmedi, DB sayacı ile masaüstü rozeti arasında kalıcı 1 fark yarattı). <br><br>Aynı anda başlatılan `reverb:start` (pid 3788) ve `schedule:work` (pid 19636) de aynı bayatlıkta. `serve` (pid 13424) istek başına kod yüklediği için etkilenmiyor. **Çözüm: daemon'ları yeniden başlatmak — kullanıcının kararı, teknik lider kullanıcının süreçlerine dokunmadı.** <br><br>**Kalıcı ders:** uzun ömürlü PHP daemon'ları koda göre bayatlayabilir; bir ölçüm turu "özellik çalışmıyor" diyorsa, önce daemon başlangıç zamanı ile ilgili commit tarihi karşılaştırılmalı. |

| ~~**O85**~~ | **JumpList / "son 5 kayıt" — YAZILDI ve KURULU UYGULAMADA ÖLÇÜLDÜ 2026-09-01** | ✅ | ❌ | ❌ | **Karar (danışman):** F5'te yazılmaz. Belirleyici olan maliyet değil **doğrulanamazlık**: JumpList girişleri `syncra://<entity>/<id>` ile açılır ve şema OS'a **yalnız paketlenmiş kurulumla** kaydedilir (O87) — dev build'de uçtan uca ölçülemez, yani F5'in kendi kabul standardıyla ("Windows'ta doğrulanmış") sınanamaz. Ayrıca **"son 5 kayıt"ın veri kaynağı yok**: uygulama son bakılan kayıtları hiçbir yerde izlemiyor; o mekanizma + `windows` crate + unsafe COM (`ICustomDestinationList`/`IShellLink`) + `cfg(windows)` dallanması gerekir (~2-4 gün). <br><br>**SYNCDESKTOP güncellendi (teknik lider):** §6.4 satır 594 ikiye bölündü ve devir gerekçesiyle yazıldı · §10 F5 madde 7 `autostart/window-state/badge (**recent → F7**)` oldu · §10 F7 kabulüne JumpList doğrulaması eklendi · §13 revizyon günlüğüne satır girdi. **Sözleşme düzeltilmeden kapsam daraltmak, O78'i doğuran hatanın ta kendisi olurdu.** |
| ~~**O86**~~ | **Deep link soğuk başlangıçta çalışmıyordu — KAPANDI 2026-09-01, GERÇEK UYGULAMADA ÖLÇÜLDÜ** | ✅ | ✅ `deep_link.rs` — `LaunchHandoff` durum makinesi (`Idle / Waiting(target) / Ready / Delivered`). Launch hedefi artık `.setup()`'ta **emit edilmiyor, tutuluyor**; `startDeepLinkBridge()` `listen()`'ı **await edip** `deep-link-ready` yayınlıyor, Rust bekleyen hedefi o an teslim ediyor. **Yeni Tauri komutu eklenmedi** — komut eklemek §6.2'yi, `check-command-wiring`'in `CONTRACT`'ını ve onları alıntılayan dokümanları tek bir string için genişletirdi; mevcut olay kanalı (`core:event:default`) zaten yeterliydi, hiçbir capability kımıldamadı (`check:commands` 30/30 sabit). **`Option` değil durum makinesi**, çünkü "hedef var" ile "webview abone" iki ayrı iş parçacığından gelir ve **iki sırada da** çalışmak zorundadır; ikinciyi kim gerçekleştirirse teslimi o yapar, `Delivered` terminal olduğu için F5/HMR ile yeniden abone olan webview aynı launch'ı ikinci kez yönlendiremez. | ✅ src-tauri **113 → 121** (+8), vitest +6. **Negatif kontrol:** eski davranışa (koşulsuz emit) döndürülünce **4 Rust + 3 TS testi kırmızı**, biri doğrudan *"nothing can be emitted yet — this is `.setup()`, the webview has no listener"* diyerek. | **Gerçek uygulamada dört senaryo, teknik lider doğruladı:** soğuk başlangıç `syncra://deal/29` → `pathname "/deals/29"` **ve kayıt yüklendi** (eskiden `/` kalıyordu) · sayfa yenilendikten sonra deep link'e **geri fırlamıyor** (tek sefer teslim) · uygulama açıkken `syncra://deal/11` → `/deals/11`, aynı süreç (single-instance korundu) · `syncra://setting/42` → rota `/` **ve** log'da `deep link rejected`. <br><br>**İki ölçüm sınırı dürüstçe kayıtta:** çalışırken gelen linkte pencerenin **öne gelmesi** ayırt edici şekilde ölçülemedi (pencere zaten odaktaydı); ve bu turda tray → Quit otomatikleştirilemediği için kapatma `taskkill` ile yapıldı, yani `RunEvent::Exit` teardown'ı **bu turda tetiklenmedi** (önceki turda üç kez doğrulanmıştı — O64). |
| ~~**O87**~~ | **`syncra://` şema kaydı — KAPANDI 2026-09-01, ikinci kez ölçüldü** | ✅ | ❌ | ❌ | **Karar (danışman):** F5'i bloke etmez. Şema kaydı doğası gereği paketleme işidir — `tauri-plugin-deep-link` Windows'ta şemayı kurulum sırasında registry'ye yazar; dev build'de yokluğu **beklenen** davranıştır. F5-4'ün ölçülen kısmı (parse + rota + single-instance + soğuk başlangıç teslimi) çalışıyor. <br><br>**SYNCDESKTOP §10 F7 kabulüne eklendi:** kurulumdan sonra `HKCU\Software\Classes\syncra` kaydının **kurulumla geldiği** doğrulanır; gelmiyorsa `deep_link().register_all()` eklenir. |
| ~~**O88**~~ | **Quick-capture Kaydet düğmesi erişilemiyordu — KAPANDI 2026-09-01** | ✅ | ✅ `QuickCapture.tsx`: düğme satırı `mt-auto` ile "kalan boşluğa" itiliyordu; içerik boşluktan uzun olunca itecek yer kalmıyor ve satır kırpılan alanın dışına taşıyordu. Artık gövde `min-h-0 flex-1 overflow-y-auto` (satır 107), eylem şeridi onun **kardeşi** ve `shrink-0` (satır 197) — yüksekliği önce ayrılıyor, taşan içerik onu kırpamıyor. | ✅ `quick-capture-layout.test.ts` (5 test) yapısal değişmezleri kilitliyor: gövde ile şerit kardeş, iç içe değil; şerit `shrink-0`, gövde `min-h-0` taşıyor. Gerçek DOM testi bu altyapıyla mümkün değil (`vitest` `environment: node`, jsdom/testing-library bağımlılık değil) — şerit bunu **uydurmak yerine açıkça belirtti** ve `quick_capture.rs`'in `include_str!` + metin-assert desenini izledi. | **Ölçülen semptom:** 480×360 pencerede form `scrollHeight 412 / clientHeight 358`, Lead ve Activity sekmelerinde Save rect'i `top 357 / bottom 397` — **tamamen görünür alanın dışında, fareyle tıklanamıyor.** Kullanıcı yalnız input'ta Enter ile kaydedebiliyordu. **Düzeltme gerçek viewport'ta ölçüldü:** dört sekmede de Save `top 303 / bottom 343`, hepsi görünür; Lead'de gövdenin gerçekten taştığı (`scrollHeight 360 > clientHeight 285`) ve şeridin etkilenmediği ayrıca doğrulandı — "şans eseri sığdı" değil. |
| ~~**O89**~~ | **Fuzz yanlış katmandaydı — KAPANDI 2026-09-01, §9/5 ✅** | ✅ | ✅ Korpus `Url::parse → as_str() → parse_deep_link` zincirinden besleniyor (plugin'in `handle_cli_arguments`'ıyla aynı `url 2.5.8`). **50 → 83 örnek**; orijinal 50 korundu, 33 normalizasyon ailesi eklendi. `deeplink-routes.ts` ikinci savunma hattı `Object.hasOwn` + `^[0-9]{1,12}$` ile kapatıldı. | ✅ src-tauri **121 → 123**, vitest **289 → 292**. **İki negatif kontrol koşuldu** ve teknik lider doğruladı: `ENTITIES`'e 9. isim → fuzz 5 testle kırmızı; TS guard'ları kaldırılınca 2 test kırmızı. | **Danışmanın saydığı 4 aileye ek 5 aile ölçümle bulundu:** percent-encoded nokta segmentleri (`%2e%2e`), şema case-folding (`SYNCRA://` kabul, ama host katlanmıyor → `DEAL/42` red), boş credential düşürme (`syncra://@lead/42` → lead/42), baş/son C0 kontrol karakterleri, id sonundaki NUL/boşluk. Kendi kontrolleri: percent-encoding **çözülmüyor**, `\` host'ta yasak (parse patlıyor), opaque host küçük harfe **çevrilmiyor**. <br><br>**Ayrıcalık artışı arandı ve sınırı çizildi:** normalizasyon **id**'yi kaydırabiliyor (`deal/1/../2` → deal 2) ama **entity**'yi asla — entity url'in host'u. Teknik lider WHATWG URL ile bağımsız doğruladı. **Düzeltilmedi, karara bağlandı** (§9/5). |
| **O90** | **Ekler masaüstünde görünmüyordu — KARAR A29, ara tur SÜRÜYOR** | ✅ | ⏳ | ⏳ | **Karar (danışman + teknik lider doğrulaması):** `attachments` sync kapsamına **girmez**, baytlar senkronlanmaz; sunucu `messages` pull satırına metadata'yı **dört düzleştirilmiş alan** olarak iliştirir (A26 deseni). Sözleşme `SYNCDESKTOP.md` §4.4 **KARAR A29** + protokol §1.3 notu olarak yazıldı. <br><br>**Kritik ölçüm — inline önizleme bugün fiziksel olarak imkânsız:** `<img src>` isteği **bearer başlığı taşıyamaz** ve masaüstü cookie göndermez (`platform/http.ts` → `transport: 'bearer'`, `withCredentials` kapalı; A12 stateful origin'i yasaklıyor), rota `auth:sanctum` arkasında → **401**. Üstüne `ChatAttachmentResource` url'i host'suz, webview origin'i `tauri.localhost`. Bu yüzden **O90a**: her ek — görsel dahil — **ad + boyut taşıyan dosya kartı** (ağ istemez, çevrimdışı görünür). **O90b**: tıkla-aç için `cache_quote_pdf` deseninde bearer'lı indirme komutu — ayrı madde. <br><br>**Durum:** backend attach adımı + fixture ✅ · crate `0004_message_attachment_fields.sql` ✅ (crate **169 → 172**) · `mapMessage` ✅ (vitest **292 → 300**) · gönderim anındaki iyimser boşluk ⏳ düzeltiliyor. <br><br>**`wire-fixtures` mekanizması ilk kez gerçek bir vakada ateşledi:** backend fixture'a `attachment_is_image` koydu, aynada kolon yoktu, `every_pulled_key_has_a_mirror_column` kırmızı verdi — *"upsert_row discards it without a word. This is exactly how the ticket SLA fields went missing."* Bir tarafın teli tek başına değiştirmesini önlemek için kurmuştuk. |
| ~~**O91**~~ | **Retention dışı kayda giden geçerli link — KAPANDI 2026-09-01 (deal detayı için; bkz. O101)** | ✅ | ✅ Sinyal **zaten yapısaldı, tüketilmiyordu**: `engine.ts:319` `MissingRowError.code = 'ROW_NOT_LOCAL'`, `writes.ts:124-133` `readBack()` onu fırlatıyor. Eklenen: `ui/errors.ts` `KNOWN_ERROR_CODES`'a `ROW_NOT_LOCAL`; `frontend/src/platform/errors.ts` (yeni) `isRecordNotMirrored(error)` — **yalnız `error.code`'a bakar, mesaj metnine asla**; `DealDetailPage.tsx:71-82` hata dalı ayrıldı; `errors.ROW_NOT_LOCAL` tr/en/de/fr | ✅ `rowAvailability.test.ts` 7 test; negatif kontrolde 2 test kırmızı (`false` ve `desktop:errors.unknown` döndü), geri alınca 7/7. `check:errors` 24/24 simetrik | Web'de tetiklenmez: `platform/web.ts` bu kodu hiç üretmiyor, fonksiyon hep `false` döner — platform dallanması yok. Mesaj iki durumu birden kapsıyor ("olabilir"): retention dışı **ve** henüz hiç çekilmemiş kayıt. `syncra://deal/12` rota olarak **doğru** gitti ama sayfa *"Something went wrong while loading the deal."* verdi. Deep link hatası değil: yerel ayna 18 deal tutuyor ve eksik olanların hepsinin `updated_at`'i ~2026-08-02'den eski — **retention penceresi**, yani tasarlanmış davranış. <br><br>Sorun mesajda: cihazda olmayan bir kayda giden **geçerli** link, *"bu kayıt çevrimdışı kopyada yok"* demek yerine genel bir hata gösteriyor. Kullanıcı bunu bozukluk sanır. §6.4 deep link maddesinin UX borcu. |

| ~~**O92**~~ | **`Syncra.log` rotasyonu arşivlemeden siliyordu — KAPANDI 2026-09-01** | ✅ | ✅ `logging.rs:43-69` `LOG_MAX_FILE_SIZE_BYTES = 5 MB` + `LOG_ROTATION_STRATEGY = KeepSome(5)`; `lib.rs:118-133` builder zincirine bağlandı, `format(masking_format)` **zincirde korundu** (§9/9) | ✅ Derleme-zamanı kilit: `const _: () = assert!(matches!(…KeepSome(n) if n > 0))`. Teknik lider negatif kontrolü koşturdu: `KeepOne`'a çevirince **E0080 — "O92: rotation strategy must archive rotated files"**, geri alınca `Finished, EXIT=0`; `cargo test` 123 passed, clippy `-D warnings` EXIT=0 | **Mekanizma kaynaktan doğrulandı** (`tauri-plugin-log 2.9.0`): `DEFAULT_MAX_FILE_SIZE = 40_000` (satır 46), `DEFAULT_ROTATION_STRATEGY = KeepOne` (47), ve `KeepOne` kolu gerçekten `fs::remove_file` — rename yok, arşiv yok. Ayrıca `KeepSome(keep)` içeride `remove_old_files(keep - 1)` çağırıyor: `KeepSome(0)` **usize taşması** olurdu, `n > 0` assert'i tesadüfen değil gerekli. Bedeli: en kötü hâlde ~30 MB log (5 arşiv × 5 MB + aktif). Deep link ölçüm turunda `deep link rejected` satırı ilk denemede log'da **görünmedi**; ikinci denemede (dosya küçükken) net yakalandı. Sebep tespit edildi: `%LOCALAPPDATA%\com.syncra.desktop\logs\Syncra.log`, `tauri-plugin-log`'un varsayılan ~40 KB boyut eşiğinde **arşivsiz** rotasyona uğruyor ve rotasyon anına denk gelen satırlar kayboluyor (`get password from entry device-token` yeni dosyanın 1. satırıydı, hemen sonraki iki senkron log çağrısı yoktu). <br><br>**Neden önemli:** bu projede "log'da X görünmeli" biçiminde kurulan her ölçüm bu yüzden **sahte negatif** verebilir — ve sahte negatif, olmayan bir hatayı kovalamaya yol açar. Ölçüm turları log'u tek kanıt olarak kullanmamalı. **Düzeltme yapılmadı**, yalnız gözlem. |

| ~~**O93**~~ | 🔴 **Gönderilen ekin kartı KAYBOLUYOR — KAPANDI 2026-09-01** | ✅ | ✅ `desktop/src/platform/data/comms.ts:273-296`: `type` artık **türetiliyor** (`attachmentId === undefined ? 'text' : 'file'`, sunucunun `MessageService::create()` kuralıyla aynı) ve `takeAttachmentMeta()` ile `attachment_name/attachment_mime/attachment_size` yerel satıra yazılıyor; metadata yükleme yanıtından tutuluyor (`rememberAttachmentMeta`, `:344/:394`) | ✅ `comms.test.ts:319` `describe('chatSource.sendMessage — attachment metadata and type (KARAR A29)')` — 3 test: kart `null` olmuyor, `type='file'` fikstürle sabitleniyor, düz mesaj bu kolonları **yazmıyor** (negatif kontrol) | **Not:** `attachment_is_image` bilinçle yazılmıyor; ayna kopyası sunucudan gelir. Aşağısı bulgu tarihçesidir: **Teknik lider ölçtürdü, danışmanın "emin değilim" dediği noktaydı — ve korkulandan kötü çıktı.** `comms.ts::sendMessage` yerel satırı yazarken dört metadata alanını **yazmıyor**, yalnız `attachment_id`. `readBack` + `mapMessage` bu yüzden `attachment: null` döndürüyor. <br><br>Akış: paylaşılan hook'un iyimser mesajı `variables.attachment` sayesinde kartı **doğru gösteriyor** → `useSendMessage`'ın `onSuccess`'i iyimser mesajı `real` ile **tamamen değiştiriyor** → `real.attachment` `null` → **kart kayboluyor**, sonraki pull turuna kadar geri gelmiyor. Kullanıcı dosyayı gönderiyor, kartı görüyor, sonra kart siliniyor. <br><br>**Çözüm elimizde:** `comms.ts` `uploadAttachment` zaten `POST /api/attachments`'tan `{data: Attachment}` alıyor ve `AttachmentResource` `original_name`/`mime_type`/`size` gönderiyor — **istemci metadata'yı gönderim anında zaten tutuyor, sadece atıyor.** <br><br>**İkinci hata aynı satırda:** `type: 'text'` **sabit**, `attachment_id` dolu olsa bile. Fixture sunucunun `"type": "file"` gönderdiğini gösteriyor → yerel satır sunucuyla çelişiyor ve ilk pull'da tip zıplıyor. |

| ~~**O94**~~ | 🔴🔴 **VERİ KAYBI: sohbete gönderilen her ek 24 saat sonra siliniyordu — KAPANDI 2026-09-01, ÖLÇÜMLE** | ✅ | ✅ `baseQuery()` artık `whereNotExists(messages WHERE attachment_id = attachments.id)` ile mesaj eklerini dışlıyor. `whereDoesntHave` **değil**: `Attachment` üzerinde `messages` ilişkisi yok (yalnız bunun için ilişki eklemek gerekirdi) ve komut tüm tabloyu taradığı için alt sorgu `messages_attachment_id_foreign` indeksi üzerinden aday başına tek arama yapıyor. `messages.deleted_at` **kasten filtrelenmedi** — soft-delete edilmiş mesaj geri alınabilir, eki hâlâ ona aittir; ayrı test kilitliyor. | ✅ **Kanıt önce yazıldı, kırmızı görüldü, sonra düzeltildi.** Gerçek HTTP yolundan uçtan uca ölçüm:<br>`ÖNCE: attachments 1 satır · disk dosyası VAR · messages.attachment_id = 1`<br>`SONRA: attachments 0 satır · disk dosyası YOK · messages.attachment_id = NULL`<br>Üç kayıp aynı anda. Yeni `PruneOrphanAttachmentsTest` 6 test (pozitif kontrol dahil: **gerçekten sahipsiz** ek hâlâ siliniyor — onsuz düzeltme "her şeyi koru"ya dönerdi). Backend **1475 → 1481**. Negatif kontrol: düzeltme geri alınınca 3 test kırmızı. | **Zincir (teknik lider kod okumasıyla buldu, şerit ölçüme çevirdi):**<br>1. `PruneOrphanAttachments::baseQuery()` = `Attachment::query()->unattached()->where('created_at','<',$cutoff)` — **`messages` tablosuna hiçbir referans yok.**<br>2. `Attachment::scopeUnattached()` = `whereNull('attachable_id')`.<br>3. `MessageService::create()` mesaj ekini bağlarken **yalnız `attachment_id`** yazıyor; `attachable_*` **hiç yazılmıyor** (depoda tek yazar `LeadConversionService`, lead→contact dönüşümü).<br>4. Cutoff varsayılanı **24 saat** (`config/chat.php:107`).<br>5. Silme `forceDelete()` **artı** `Storage::disk()->delete()` — satır **ve disk dosyası**, geri dönüşü yok.<br>6. `routes/console.php:59`: `Schedule::command('attachments:prune-orphans --force')->dailyAt('03:47')` — **canlı zamanlı.** <br><br>**İKİNCİ YALAN SÖYLEYEN DOCBLOCK:** komutun kendi başlığı (satır 12-17) *"zamanlayıcıya KAYITLI DEĞİL ve hiçbir yerden otomatik çalışmaz"* diyor. Satır 59 onu yalanlıyor. Birincisi `SyncPullService`'in K7 iddiasıydı; bu ikincisi çok daha pahalı. <br><br>**Yan etki:** `messages.attachment_id` FK'sı `nullOnDelete` → silme, kapsam içindeki `messages` satırını **`sync_version` artırmadan** değiştiriyor; ayna olmayan bir eke ait kartı süresiz taşır (probe D3 sınıfı). <br><br>**Bu bulgu görev tanımının DIŞINDA çıktı:** threat model şeridi A29'un *"AttachmentPolicy ile birebir aynıdır"* iddiasını doğrularken zincire denk geldi. Aynı şerit o iddiayı da çürüttü — sonuç doğru, **gerekçe yanlış** (policy bugün `SyncScope`'tan daha dar, çünkü `attachable_id` hiç yazılmıyor). |
| ~~**O95**~~ | **CI release-only kodu hiç derlemiyordu → smoke adımı EKLENDİ ve CI'DA YEŞİL KOŞTU** | ✅ | ✅ `desktop-ci.yml` `desktop-release-smoke` (yalnız Windows) | ✅ **2026-09-01, CI'ın kendi çıktısı:** `Started …/release/syncra-desktop.exe (pid 8328); waiting up to 10s...` → `Still running after 10s - release binary starts cleanly.` İş 20:03→20:34 (release build tek başına 28 dk 16 sn) | Script'in kendisi de korunaklı ve gerekçesi yorumda: `$ErrorActionPreference='Stop'` **zorunlu** — yokluğunda başarısız bir `Process::Start` sonrası `$proc` null kalıyor, koşul false değerlendiriliyor ve script **sessizce başarı dalına** düşüp 0 dönüyordu; bir kez tam olarak öyle üretilmiş. Ayrıca `$null -eq $proc` kontrolü ve mutlak yol (göreli yol `Test-Path`'i geçip `ProcessStartInfo`'da başlatılamıyor). Artık "beklenen kırmızı" değil, **ölçülmüş yeşil**. |
elease\syncra-desktop.exe (pid 8328); waiting up to 10s...` → `Still running after 10s - release binary starts cleanly.` İş 20:03→20:34 (release build tek başına 28 dk 16 sn). Script'in kendisi de korunaklı: `$ErrorActionPreference='Stop'` (yokluğunda başarısız `Process::Start` sonrası koşul false olup **sessizce yeşile** düşüyordu — bir kez öyle üretilmiş ve yorumda kayıtlı), `$null -eq $proc` kontrolü, mutlak yol | Artık "beklenen kırmızı" değil, **ölçülmüş yeşil**. |
| ~~**O96**~~ | **Boşta RAM kriteri ölçülemezdi — TANIM DÜZELTİLDİ 2026-09-01** | ✅ | — | — | §10 F7 *"boşta RAM < 150 MB"* diyordu ama **neyi saydığı yazmıyordu** ve iki okuma arasında 12 kat fark vardı: yalnız `syncra-desktop.exe` **39 MB**, tüm ağaç (ana + 6 WebView2 süreci) **462 MB** WS / **241 MB** Private. <br><br>**Yeni kriter:** oturum açık + sync çalışır, 3 dk boşta, **toplam Private Bytes < 400 MB**. Working Set **kabul ölçütü değil** — paylaşılan WebView2 DLL'lerini süreç başına mükerrer sayıyor. Yalnız ana süreci saymak da kabul değil (kullanıcının ödediği bellek ağacın tamamı). **150 MB, WebView2'nin taban maliyetinin altındaydı — hiçbir sürümde tutmayacak ölü bir kriterdi.** <br><br>**Açık:** oturum açıkken hiç ölçülmedi (O12 yüzünden uygulama açılamıyordu). Ölçüm 400'ü aşarsa **önce kod sorgulanır**, eşik değil. |
| ~~**O97**~~ | **NSIS sevk edilen kurulum; MSI `perMachine` kalıyor — KARARA BAĞLANDI 2026-09-01** | ✅ | — | ✅ ölçüldü | **Ölçüldü:** NSIS (`currentUser`, yönetici istemez) `HKCU\Software\Classes\syncra` yazıyor ve OS `syncra://deal/29`'u doğru exe'ye yönlendiriyor → **defter O87 "evet"le kapandı, `register_all()` gerekmiyor.** MSI yükseltilmemiş oturumda **1603** ile reddedildi ve `HKLM`'e yazıyor. <br><br>**Teknik lider bir çelişkiyi çözdü:** paketleme şeridi "`main.wxs` satır 115" dedi, danışman "özel `main.wxs` yok" dedi. **İkisi de doğru** — repoda özel şablon yok; okunan dosya `desktop/target/release/wix/x64/main.wxs`, yani Tauri'nin gitignore'lu build dizinine kendi ürettiği şablon. Yani `perMachine`/`HKLM` **proje kararı değil, Tauri varsayılanı** — bu, şablonu fork'lamama kararını güçlendirdi. <br><br>MSI kurumsal/GPO dağıtımı için üretilmeye devam ediyor; **HKCU şartı MSI'ya uygulanmıyor.** §10 F7 buna göre düzeltildi. |
| ~~**O98**~~ | **`rust-version` beyanı ve CI apt listesi — KAPANDI 2026-09-01, CI ile DOĞRULANDI** | ✅ | ✅ iki manifest `1.89`; Linux bağımlılıkları artık `.github/scripts/linux-runner-prep.sh` içinde, **tek kaynak** | ✅ Üç OS'ta da yeşil: `rust workspace` (windows · ubuntu · macos), `tauri build --debug` (üçü), `release smoke` | Liste iki işte ayrı ayrı dururken bir commit içinde **drift etti** (O115/3); tek dosyaya indirilmesinin sebebi bu. |
| **O99** | **Ertelenen iki küçük madde: uninstall kalıntısı + üçüncü `is_image` kopyası** | ✅ | ❌ | ❌ | **(a) Uninstall kalıntısı:** NSIS uninstaller `HKCU\Software\syncra\Syncra` (kurulum-yolu izi) bırakıyor. **Ertelendi** — temizlemek özel NSIS template fork'u gerektirir (MSI için reddedilen aynı bakım yükü); kalıntı işlevsel değil, veri/gizlilik içermiyor, yeniden kurulumda üzerine yazılıyor. Tauri upstream düzeltirse bedava kapanır. <br><br>**(b) `ChatAttachmentResource.php:48`** hâlâ `str_starts_with($mime,'image/')` — üçüncü kopya, tüketicisi `MessageBubble.tsx:259`. **Teknik lider severity'i ÖLÇTÜ ve DÜŞÜRDÜ:** `AttachmentController::show()` `$disposition`'ı zaten `isInlineEligibleImage()` ile belirliyor → SVG **asla inline servis edilmiyor**, üstüne `X-Content-Type-Options: nosniff`. **XSS yolu yok**; kalan etki kırık `<img>` + K7 tanım ayrışması. **Düzeltme web reposunda yapılmalı** (danışman): burası masaüstünün reposu, web'in ayrı reposu var; burada yamalamak iki repoyu ayrıştırır ve web'in kendi test/faz disiplini dışında bir değişiklik üretir. |
| ~~**O100**~~ | **`secrets.DESKTOP_RELEASE_ENV` — KURULDU ve ÇALIŞTI 2026-09-02** | ✅ | ✅ secret yazıldı; `Seed frontend/.env` adımı onu okuyup `check:release-host`'u geçiriyor | ✅ İlk gerçek release koşumunda ölçüldü: `DESKTOP_RELEASE_ENV byte length: 145` + `seeded 5 VITE_* lines into frontend/.env`, ardından assert **geçti** | İlk denemede secret **boş** gelmişti ve `[ -n ]` bunu "set değil"den ayıramıyordu; bayt-uzunluğu teşhisi (O115) tam bu yüzden eklendi ve ikinci turda cevabı tek satırda verdi. **Değer `crm.example.com`** — RFC 2606 dokümantasyon alan adı, yani bu sürüm bilinçli olarak bir doğrulama turudur, sevk edilmiş bir ürün değil. |
| **O101** | **O91'in çözümü yalnız deal detayına uygulandı** | ✅ | ⚠️ kısmi | ⚠️ | `isRecordNotMirrored()` + `desktop:errors.ROW_NOT_LOCAL` altyapısı **genel** (`readBack()` katmanında), ama UI dallanması yalnız `DealDetailPage.tsx`'te. `contacts`/`companies`/`leads`/`tickets`/`quotes` detay sayfaları aynı `readBack()` yolundan geçiyor ve aynada olmayan kayıt için hâlâ genel *"bir şeyler ters gitti"* gösteriyor. Bilinçli kapsam sınırı (bug raporu deal linkiydi), ama **aynı hatanın beş kopyası duruyor**. Ortak bir `<RecordLoadError>` bileşeni doğru çözüm olabilir. |
| ~~**O102**~~ | **§8 "çevrimdışında devre dışı + tooltip" — BAĞLANDI 2026-09-01 (16/16 fiil; 4 rota-seviyesi madde açık, bkz. O109)** | ✅ | ✅ İki katman: **(a)** 16 UI tetikleyicisi çevrimdışıyken devre dışı + `desktop:onlineOnly.<action>` tooltip'i (leads convert/import, quotes send/revise/pdf, users.*, attachments upload, savedViews create/update, exchange rates, password change) · **(b)** savunma hattı: 16 §8 fiili `requireOnline()` ile sarıldı, `OnlineOnlyError` dönüyor, UI eylem-özgü metni gösteriyor. Paylaşılan çekirdek `frontend/src/platform/onlineOnly.ts` + `useOnlineOnly.ts`; **`isDesktop` dallanması yok** — karar hatanın **şekli** üzerinden (`SyncStateBadge`/`isRecordNotMirrored` deseni). Sözlüğe **tek anahtar eklenmedi**, 17'si zaten vardı | ✅ Teknik lider koştu: `npm test` **369 passed (17 dosya)**, `check:data` → **"§8 methods: 16, all bound to http"**, `check:errors` 24/24, `tsc` temiz. Yeni `onlineOnly.test.ts` üç şeyi kilitliyor: tablo `SPEC_8_METHODS`'u **birebir** kapsıyor · çevrimdışıyken kaydedilen HTTP çağrısı **sıfır** ("ağ patladıktan sonra reddettik" ile "ağa hiç dokunmadık"ı ayıran şey) · `web.ts`'in `onlineOnly`'si hâlâ kimlik fonksiyonu (web davranışının değişmediğinin yapısal kanıtı) | Temizlik turunda yol boyunda bulundu. Üç parçanın ikisi hazır, üçüncüsü yok: **sözleşme** `frontend/src/platform/types.ts:732` `onlineOnly<T>(action, fn)` + `OnlineOnlyError` (`types.ts:156`) ✅ · **metin** `desktop:onlineOnly.*` **17 anahtar × 4 dil** ✅ · **bağlama** ❌. <br><br>**Ölçüm:** `desktop/src/platform/desktop.ts` içinde `onlineOnly(` çağrı sayısı **0** — yani hiçbir fiil sarılmıyor; `OnlineOnlyError`'ı tüketen tek bir bileşen yok (grep: yalnız tip tanımı ve bir yorum). `i18n:dead-keys` 17 anahtarın hepsini **referanssız** raporluyor. <br><br>**Neden önemli:** SYNCDESKTOP §8 F4 kabulünün parçasıydı ("§8 listesi"). Bugün çevrimdışı bir kullanıcının `quotes.send` gibi bir fiile bastığında **ne gördüğü ölçülmedi** — `StorageSettings.tsx:186` yorumu genel `errors.OFFLINE`'a işaret ediyor, yani muhtemelen eylem-özgü "neden" metni yerine genel bir hata. **Bu 17 anahtar ölü değil, bağlanmamış** — silmek §8'in borcunu görünmez kılardı. |
| ~~**O103**~~ | **F7 ÖLÇÜM TURU — dört kriterin dördü de GEÇTİ (2026-09-01)** | ✅ | ✅ | ✅ | Kurulu **NSIS** paketi üzerinde teknik lider ölçtü (tahmin yok):<br>• **MSI boyutu:** `Syncra_0.1.0_x64_en-US.msi` = **12,92 MB** (13.549.568 bayt) — kriter < 25 MB **GEÇTİ**. NSIS setup 7,56 MB.<br>• **Soğuk başlangıç deep link (O86):** uygulama kapalı → `Start-Process "syncra://deal/29"` → uygulama açıldı **ve kaydın detayına gitti** (ekran görüntüsüyle doğrulandı: *Fırsatlar / Entegrasyon Çalışması — Toros*, ₺313.051,00). Durum çubuğu "Çevrimiçi · Son senkron: 28 saniye önce" — sync döngüsü de çalışıyordu. **GEÇTİ**<br>• **Şema kaydı (O87):** `HKCU\Software\Classes\syncra\shell\open\command` = `"…\AppData\Local\Syncra\syncra-desktop.exe" "%1"` — NSIS yazıyor, `register_all()` **gerekmiyor**. **GEÇTİ**<br>• **Boşta RAM (O96 kriteri):** oturum açık + sync çalışır, 3 dk boşta, **7 süreçlik ağacın toplam Private Bytes**'ı: t=0s **214,2** · t=90s **211,1** · t=180s **212,3 MB** (Working Set ~435 MB). Kriter < 400 MB **GEÇTİ**, üç noktada düz — sızıntı sinyali yok.<br>&nbsp;&nbsp;Kırılım O96'nın gerekçesini doğruluyor: **ana süreç yalnız 8 MB**, kalan ~204 MB altı WebView2 sürecinde (88,2 · 47,9 · 43,1 · 13,1 · 9,2 · 2,8). Yalnız ana süreci saymak anlamsız, eski "<150 MB" kriteri WebView2 tabanının altındaydı.<br><br>**Kalan:** O85 (JumpList — kod yok) ve CI'ın gerçekten koşması (push kullanıcıda). |
| ~~**O104**~~ | **Crate testleri geçici ayna dizinlerini sızdırıyordu — KAPANDI 2026-09-01, ve F8'in ön koşulunu ortaya çıkardı** | ✅ | ✅ `tests/common/mod.rs:153-182` `impl Drop for Harness` — `TempDir` düşmeden **önce** `engine.shutdown()`; `encryption.rs`/`retention_storage.rs`'te üç `drop(h.engine)` → `shutdown()` | ✅ Teknik lider ölçtü: `cargo test` → **174 passed**, `%LOCALAPPDATA%\Temp\.tmp*` sayısı **255 → 255, LEAKED = 0**. İki yeni test: `shutdown_frees_the_data_directory_for_the_f8_migration` ve `dropping_the_last_engine_clone_also_releases_the_database_files` (172 → 174) | **KÖK NEDEN (ölçüldü, hipotez doğrulandı ama daha dar):** sızıntı tüm suite'te değil, **tek dosyada 13 testin 4'ünde** — `start_background_sync()` çağırıp `shutdown()` çağırmayanların tam olarak kendisi. Mekanizma: `SyncScheduler::stop()` yalnızca `abort()`'tur ve **senkron değildir**; task'ın tuttuğu `SyncEngine` klonu (`Arc<Inner>`) test gövdesi `Harness`'ı düşürdükten sonra da yaşar, `Connection` kapanmaz, `TempDir::drop`'un `remove_dir_all`'ı Windows'ta **OS hata 32** alır ve `TempDir` bu hatayı **sessizce yutar**. Negatif kontrol tam bu hatayı üretti. <br><br>**F8 SONUCU — asıl kazanç bu:** `shutdown()` deterministik (`PRAGMA wal_checkpoint(TRUNCATE)` + `conn.close()`), ama **`stop()` tek başına yetmez**. Yani §10 F8/1'in "eskiyi sil" adımı, taşımadan önce `shutdown()` çağrılmazsa Windows'ta rastgele düşerdi — kabul kriteri ("eski dizin boş") sağlanamazdı. Bu **SYNCDESKTOP §10 F8'e zorunlu madde olarak yazıldı**, kopyalanacak dosya listesiyle birlikte (`syncra.db` + varsa `-wal` + `cache/`; `-shm` gereksiz). `lib.rs`'in `RunEvent::Exit` yolu sırayı zaten doğru uyguluyor. <br><br>**Temizlik:** teknik lider 249 dizini sildi (**118,7 MB**), içeriğini `syncra.db` ailesiyle sınırlı olduğunu doğruladıktan sonra; kalan 6 dizin boş ve bize ait değil, dokunulmadı. <br><br>**Ölçüm (teknik lider):** `%LOCALAPPDATA%\Temp` altında **245 adet `.tmp*` dizini, toplam 113,9 MB**, her biri bir `syncra.db` (≈488 KB) taşıyor. En eski **2026-08-15**, en yeni **2026-09-01 16:00** — her crate test koşumunda birikiyor. Kodda `into_path()` / `.keep()` / `forget(` **hiç geçmiyor** (grep 0), yani bilinçli bir sızdırma değil. <br><br>**Hipotez (doğrulanacak):** Windows'ta `TempDir::drop` açık dosya tutamacı olan dizini silemez ve **hatayı sessizce yutar**; SQLite bağlantısı (`-wal`, `-shm` dahil) TempDir düşmeden kapanmıyor. <br><br>**Asıl önemi disk değil, F8:** §10 F8/1 taşıma prosedürü *"engine kapalı, WAL checkpoint, `syncra.db` ailesi + `cache/` kopyala, doğrula, **eskiyi sil**"* diyor. Motor tutamakları deterministik bırakmıyorsa bu prosedürün **son adımı Windows'ta başarısız olur** ve F8 kabulü ("eski dizin **boş**") sağlanamaz. Yani bu bir test hijyeni maddesi gibi görünüyor ama bir sonraki fazın kabul kriterini önceden bloke ediyor. |
| ~~**O108**~~ | 🔴 **TM-F16: `createUpdaterArtifacts` yoktu → imzalanacak hiçbir şey üretilmiyordu — KAPANDI 2026-09-01** | ✅ | ✅ `tauri.conf.json` `bundle.createUpdaterArtifacts: true`; `desktop-ci.yml`'nin imzasız debug bundle işi `--config scripts/ci-unsigned-bundle.json` ile **yalnız kendisi için** alanı kapatıyor | ✅ Üçü de ölçüldü: imzasız **release** bundle → *"A public key has been found, but no private key"* **exit 1** · imzasız **debug** bundle → aynı hata, **exit 1** (yani düzeltme CI'ı kırardı) · overlay ile debug bundle → **EXIT 0**, iki bundle üretildi | F6 şeridi buldu, **teknik lider kendi kanıtıyla doğruladı**: ürettiğim 2026-09-01 bundle'ında tam olarak bir `.msi` ve bir `-setup.exe` vardı, **tek bir `.sig` yok**. Varsayılan `false` (`tauri-utils/src/config.rs:1540-1544`). Yani `desktop-release.yml`'nin `includeUpdaterJson: true` bayrağının imzalayacağı hiçbir şey oluşmuyordu ve **O12 ile girilen updater bloğu hiçbir zaman var olmayacak bir manifest'i işaret ediyordu** — updater "kapandı" görünürken gerçekte hiç güncelleme alamazdı. <br><br>**Yol boyunda ikinci ölçüm:** `tauri.conf.json` şeması ek özellik kabul etmiyor — gerekçeyi `"//createUpdaterArtifacts"` yorum anahtarı olarak koymayı denedim, build *"Additional properties are not allowed"* ile reddetti. Gerekçe bu deftere ve §6.5'e yazıldı. <br><br>§9/8'in kalan yarısının **ön koşuluydu**; artık ilk gerçek imzalı sürümde ölçülebilir. |
| **O109** | **§8'in dört rota-seviyesi maddesi hâlâ bağlanmadı** | ⚠️ | ❌ | ❌ | O102 §8'in fiil-seviyesi 16 maddesini kapattı; `settings.*`, `reports.*`, `dashboard.*`, `logs.*` **açık kaldı** ve gerekçe sağlam: bunların tetikleyicisi bir buton değil **bir rota**. "Devre dışı + tooltip"in burada ne demek olduğu (sol menü öğesini kapatmak mı, sayfayı boş göstermek mi, sekmeyi mi) §8'de **yazmıyor** — bu bir tasarım kararı, ve `AppShell`/sidebar yüzeyini değiştirmeyi gerektiriyor. <br><br>**Kontrolör bunu gizliyordu:** `i18n:dead-keys` `onlineOnly.*` sayısını 17 → **0** gösterdi, ama bu 0 **fazla iyimser** — script şablon anahtarlarını **önekle** eşliyor, yani tek bir `` t(`desktop:onlineOnly.${action}`) `` on yedi yaprağı birden "kullanılıyor" sayıyor. Teknik lider doğruladı: bu dört eylem yalnızca `OnlineOnlyAction` union bildiriminde geçiyor, hiçbir yerden **geçirilmiyor**. <br><br>**Kontrolör düzeltildi (teknik lider):** `check-i18n-dead-keys.mjs`'e üçüncü bir kova eklendi — *"reached ONLY via a prefix : N — counted live, not proven live"* (`--list-prefix-only` ile listelenir). Önek kuralı **kaldırılmadı** (kaldırmak `notificationText.ts` gibi gerçek tüketicileri yanlış pozitife boğardı); yapılan şey, bir anahtar ailesinin rapordan **görünmez olmasını** engellemek. Bugünkü ölçüm: 57 referanssız + **188 yalnız-önek**. |
| ~~**O110**~~ | **F8/3 KARAR TURU YAPILDI — bağlayıcı bir öncül çürütüldü** | ✅ | — (karar turu, kod yok) | ✅ kaynak kanıtı | D-3 ve §10 F8/3, *"Tauri'de CSP runtime'da değiştirilemez, dolayısıyla runtime yapılandırılabilir API URL'i `connect-src` jokerini zorunlu kılar"* varsayımına dayanıyordu. **Yanlış ikilemmiş.** Keşif turu ölçtü, **teknik lider `tauri 2.11.5` kaynağında birebir doğruladı**: `manager/mod.rs:371` politikayı her sayfa servisinde `config.app.security.csp`'den okuyor · `protocol/tauri.rs:183` HTTP response header'ı olarak basıyor · `lib.rs:411` `pub fn config_mut(&mut self) -> &mut Config`. <br><br>**KARAR: launch-time CSP sentezi (d3).** Origin'in *ne zaman* bağlandığı değişir (derleme → başlatma), *ne kadar dar* olduğu değişmez — `connect-src` tam origin çifti kalır, joker hiçbir yönergede yok. §6.3'e D-3 rev.2 olarak, §10 F8/3'e karar metni olarak yazıldı. <br><br>**Reddedilenler:** (b) `connect-src` genellemesi — bugünkü beş XSS katmanının (HTMLPurifier fail-closed · `dangerouslySetInnerHTML` fiili kullanım **0** · tek render noktası `<iframe sandbox="">` · script'te `unsafe-inline` yok · token yalnız bellekte) arkasındaki *"XSS olsa bile kanal dar"* değişmezini satardı. (c) HTTP'yi Rust'a taşımak — **hedefe zaten ulaşmıyor**: Reverb websocket'i webview'da, `img-src` host'a bağlı; `tauri-plugin-http` CSP'yi baypas ediyor ama scope'u `https://**` yapmak aynı kanalı IPC üzerinden geri açıyor. <br><br>**Kalan belge borcu:** `docs/DESKTOP-ARCHITECTURE.md` §11 D-3 ve `docs/DESKTOP-THREAT-MODEL.md` TM-F6 gerekçesi + yeni TM-F17. |
| **O111** | 🔴 **Self-host build'i, resmî sürüm yayınlandığında BİZİM build'imizle değiştirilir** | ✅ | ⚠️ README uyarısı kondu | ❌ | F8/3 turunda ortaya çıktı ve **bugün canlı**: `tauri.conf.json` **tek sabit** updater endpoint'i (bizim GitHub Releases `latest.json`'ımız) ve **bizim** pubkey'imizi taşıyor. Self-host rehberini izleyip kendi build'ini üreten biri ikisini de devralıyor. İlk resmî sürümde onun kurulumu bizim endpoint'i yokluyor, **zaten güvendiği** bir anahtarla imzalanmış güncellemeyi buluyor ve `installMode: "passive"` ile kuruyor → kullanıcıları bizim backend'imize göre derlenmiş bir binary çalıştırmaya başlıyor, kendi sunucularına ulaşamıyorlar. <br><br>**Bu, per-backend build modeline karşı en güçlü pratik argüman** ve O110'un kararını doğruluyor. Kalıcı çözüm F8/3'ün uygulanmasıdır. Bu turda yapılan: iki README'ye uyarı + iki geçici çare (updater bloğunu kaldır **ya da** kendi endpoint+anahtarınla değiştir). **Kod tarafında bir koruma yok** — bir self-hoster uyarıyı okumazsa aynı şey olur. |
| **O112** | **`VITE_REVERB_APP_KEY` de build-time gömülü — F8/3'ün çözülmemiş alt kararı** | ⚠️ | ❌ | ❌ | F8/3 karar turunda gözden kaçmış bağımsız bir alt karar: launch-time yapılandırma API URL'ini ve Reverb host'unu çözer, ama **Reverb app key'i de** `frontend/.env`'den derleme zamanında geliyor. Self-host'ta bunun sunucudan keşfedilmesi gerekir (ör. public bir config ucu) ya da yapılandırma ekranının parçası olmalı. F8/3 uygulanmadan önce karara bağlanmalı, yoksa "tek resmî build herkese yeter" vaadi realtime'da kırılır. |
| ~~**O113**~~ | **O85 ELLE DOĞRULAMA — kurulu NSIS paketinde ölçüldü (teknik lider)** | ✅ | ✅ | ✅ | Şerit 29 Rust + 19 vitest testi ve 9/9 negatif kontrol teslim etti (`cargo test` **162**, vitest **388**, `check:commands` 31/31, `check:identifier` AUMID dahil OK — hepsini teknik lider yeniden koştu). Ama COM yolunun kendisi birim testle gözlemlenemez; paketleyip kurdum ve ölçtüm:<br>• **Kısayol AUMID** `com.syncra.desktop` ✅ (süreç tarafı artık `run()`'ın ilk satırında deklare ediliyor — O106'nın kapanışı)<br>• **Altı kayıt açıldı** (deal/29 · ticket/3 · contact/7 · company/2 · lead/5 · quote/4, deep link ile). `recent.json`: **tam 5 giriş**, en yenisi başta, **altıncı (`deal/29`) düşmüş** — kapak çalışıyor. Kategori **yerelleştirilmiş** (`"Son kayıtlar"`). Başlıkların ikisi aynadan gerçek metin (`Teklif — Bakım Anlaşması`, `Rapor ekranı çok yavaş açılıyor`), ikisi fallback (`Firma #2`, `Kişi #7`) — **iki yol da tetiklendi**. `recent.json.tmp` kalıntısı **yok** (atomic yazım temiz).<br>• **Windows listeyi KABUL ETTİ:** `%APPDATA%\Microsoft\Windows\Recent\CustomDestinations` **21 → 22**, yeni `d34a3842cd2c11a0.customDestinations-ms` (9,1 KB) yazıldı. Boş değil — COM zinciri (`SetAppID` → `BeginList` → `AppendCategory` → `CommitList`) gerçekten koştu.<br>• **O107 temizlik kancası UÇTAN UCA kanıtlandı:** `clear()`'ı koşturan testlerden sonra o dosya **kayboldu** (22 → 21). Yani kayıt başlıkları OS'un **düz-metin** deposundan gerçekten siliniyor, yalnız `recent.json`'dan değil.<br>• **Açılış rebuild'i:** depo silinmişken `recent.json` dururken uygulama açıldı → depo **geri geldi** (9,1 KB, 19:28:30). `.setup()` yolu çalışıyor.<br><br>**ÖLÇÜLMEYEN, dürüstçe ayrılıyor:** menünün **görsel** hâli (sağ tık → 5 giriş ve başlık), bir girişe **tıklayınca** kaydın açılması, *"Remove from this list"*e saygı. Üçü de insan etkileşimi ister. Dolaylı kanıt güçlü (liste kabul edildi ve boş değil; girişlerin hedefi soğuk başlangıçta zaten doğrulanmış deep-link yolunun aynısı) ama **kanıt değil**. |
| **O114** | **F8/1 veri konumu taşıma — KOD ve OTOMATİK TESTLER TAMAM; uygulama içinden gerçek taşıma ÖLÇÜLEMEDİ** | ✅ | ✅ `data_dir.rs` (yeni) + `storage::{data_location, move_data_dir}` + `state.rs`'te motor/yollar `RwLock` arkasına alındı (`adopt()`) + Storage sekmesinde "Veri konumu" alanı + 12 anahtar × 4 dil. Prosedür sırası bağlayıcı: doğrula → **`shutdown()`** → kopyala → **doğrula (hedefteki DB'yi anahtarla aç ve oku)** → tercihi kaydet → eskiyi sil → yeni kökle aç | ✅ Teknik lider koştu: `src-tauri` **186** (taban 162, +24), crate **174**, vitest **388**, `check:errors` **27/27**, `check:commands` **33/33 · deferred 0 · dead 0**, `tsc`/i18n/build'ler yeşil | **Şeridin üç iyi kararı:** (1) dosya listesi yerine **kökün tamamı** kopyalanıyor — `-wal` atlansa çökerek biten koşumun *commit edilmiş* satırları sessizce kaybolurdu, ve negatif kontrol tam bunu ölçüyor (`a_copy_that_drops_the_wal_loses_committed_rows_and_fails_verification`, satır sayısı farkıyla reddediliyor). (2) Kök tercihi **aynanın içine** yazılamazdı (tavuk-yumurta) — `app_data_dir()/data-location.json` çapa dizininde, taşınmayan yerde. (3) K15'in dayanağı **doğrulandı, varsayılmadı**: SQLCipher anahtarı `keychain_service`'ten türüyor, yola hiç bakmıyor; test hem aynı anahtarın yeni adreste açtığını hem farklı anahtarın açamadığını iddia ediyor. <br><br>**Teknik lider'in ölçtükleri:** yeni build kuruldu, oturum ve sync canlı (regresyon yok) · `data-location.json` varsayılan kökte **yok** (beklenen) · **§9/4 iki taraflı tarama:** veri kökünde yalnız `syncra.db*`, `cache/quotes/4-1.pdf`, `recent.json`; isim eşleşmesi **0**. İçerik taraması `syncra.db-wal`'da bir eşleşme verdi — **yanlış pozitif olduğu kanıtlandı**: `BEGIN PRIVATE KEY` 0, `Bearer` 0, tek `eyJ` eşleşmesi ikili çöp içinde 4 karakterlik tesadüf, ve 470 KB'da **tek bir düz-metin SQL/şema izi yok** (`CREATE TABLE`/`sqlite_`/`outbox`/`desktop_settings` → 0). <br><br>**ÖLÇÜLEMEYEN, dürüstçe ayrılıyor:** *uygulama içinden gerçek taşıma*. Klasör seçici **yerel bir Windows diyalogu**; güvenilir şekilde süremem. Mekanizma otomatik testte gerçek `SyncEngine` + gerçek dosya sistemiyle uçtan uca kanıtlı (eski dizin **gerçekten siliniyor**, yeni konumdaki DB aynı anahtarla açılıyor), ama "kurulu uygulamada taşıdım" iddiasını komut+çıktı olmadan kurmam. Kullanıcının koşacağı prosedür şeridin raporunda; gerçek ayna `%USERPROFILE%\syncra-f8-backup`'a yedeklendi (5 dosya, 1,17 MB). <br><br>**Sapma kayda geçiyor:** §10 F8/1 *"`dialog:allow-open` ile dizin seçimi"* diyor; capability **var** ama JS tarafı ona ulaşamıyor (`@tauri-apps/plugin-dialog` `desktop/package.json`'da yok). Seçici **Rust tarafında** (`blocking_pick_folder`) — `clipboard.rs`'in belgelenmiş deseni. `move_data_dir(target)` açık yol da kabul ettiği için ileride JS tarafı eklenirse komut değişmiyor. <br><br>**Ölçülmeyen kısıt:** gerçek uzak SMB/NFS. Şerit loopback UNC ile ölçtü ve SQLite **reddetmedi** — yani hata sessiz olurdu; bu yüzden çalışma-anı probu yerine **statik "yalnız sabit disk"** kısıtı kondu (`GetDriveTypeW`, yalnız `DRIVE_FIXED`). Muhafazakâr ve gerekçeli. |
| ~~**O115**~~ | **CI ilk kez UÇTAN UCA YEŞİL — dört turda, üçü benim hatam** | ✅ | ✅ | ✅ | **Sonuç (2026-09-01, run 33550538821):** on işin onu da başarılı — `rust workspace` ×3 OS · `tauri build --debug` ×3 OS · `release smoke (windows)` · backend · frontend · cargo audit. Bu dalın tarihinde **`tauri build` ve `release smoke` ilk kez koştu**. <br><br>**Dört turun anatomisi, kayda değer çünkü üçü aynı sınıftı:** <br>1. `-lgbm` — apt listesi eksikti. **Bu bir başarıydı**: geçen tur "listeyi şişirme, eksik paket adıyla patlasın" diye bilinçle karar verilmişti ve paket tam olarak adını söyledi. Mekanizma kaynaktan doğrulandı (`gbm-sys 0.4.0/src/lib.rs:13` düz `#[link(name="gbm")]`), kardeş `-sys` crate'leri de kontrol edildi, **tek** paket eklendi. <br>2. macOS'ta üç clippy hatası — F8/1'in `cfg(windows)` ile elenen kollarında. Şerit riski **önceden yazmıştı** ("kesin kanıt CI'ın 3-OS matrisidir"). CI logundan yamanmadı: `cfg(windows)`→`cfg(unix)` çevirisiyle koşul **yerelde üretildi**, aynı üç hata aynı satırlarda görüldü, düzeltildi. <br>3. 🔴 **Teknik lider hatası:** `libgbm-dev` iki apt listesinden **yalnız birine** eklendi; bundle işi tam olarak düzeltmenin yazıldığı boşlukla kaldı. Yerinde yamamak yerine iki liste tek script'e indirildi — üstelik listelerden birinin üstünde *"keep the two lists identical"* yazıyordu, yani mekanizmanın yerini almış bir insan talimatı. <br>4. 🔴 **Teknik lider hatası:** o script göreli yolla çağrıldı; iki işte de `defaults.run.working-directory: desktop` var, yol `desktop/.github/...`'e çözüldü, **exit 127**. Yerini alan inline bloklar yalnız mutlak komutlar (`apt-get`, `df`, `rm`) çağırdığı için çalışma dizini o adımlarda hiç önemli olmamıştı. <br><br>**Ders:** 3 ve 4'te de değişikliği workflow'un *yazdığına* göre yazdım, runner'ın onunla *ne yaptığına* göre değil. **YAML ayrıştırma ve `bash -n` ikisinde de yeşil geçti** — ikisi de sözdizimine bakıyor, bağlama değil. Bir workflow adımı eklerken kontrol edilecek şey sözdizimi değil, o işin `defaults` ve mevcut duplikasyonlarıdır. <br><br>**Yol boyunda ölçülen:** Linux runner'ın gerçek payı ilk kez görüldü — `13G` boş (%83 dolu) → temizlik sonrası `31G` (%57), yani **18 GB** kazanıldı. `CARGO_INCREMENTAL=0` + `CARGO_PROFILE_DEV_DEBUG=0` de yerelde ölçülen 45,6 GB'lık ağacın iki en büyük payını hedefliyordu. |
| ~~**O116**~~ | 🔴 **`includeUpdaterJson` geçerli bir action girdisi DEĞİLDİ — `latest.json` hiç üretilmeyecekti** | ✅ | ✅ `desktop-release.yml` → **`uploadUpdaterJson: true`** | ✅ İlk release koşumunun logu: `##[warning]Unexpected input(s) 'includeUpdaterJson', valid inputs are ['releaseId','tagName',…,'uploadUpdaterJson','updaterJsonPreferNsis',…]` | **O108'in birebir aynısı, bir katman yukarıda.** `SYNCDESKTOP.md` §6.5 *"`desktop-release.yml` zaten `includeUpdaterJson: true` ile bu asset'i üretiyor"* diyordu; pinlenmiş `tauri-action` sürümü o ismi **tanımıyor**, uyarı basıp yok sayıyordu. Yani updater bloğu (O12) ve artifact üretimi (O108) düzeltildikten sonra bile, uygulamanın çekeceği **manifest hiç oluşmayacaktı** — ve bunu ancak gerçek bir release koşumu gösterebilirdi. `updaterJsonPreferNsis` ismi doğruymuş. <br><br>**Ders:** bir action'a verilen girdi adı, tıpkı bir komut adı gibi sözleşmedir — ve bu depo komut adları için üç kaynaklı bir kontrolör (`check:commands`) yazmıştı; workflow girdileri için karşılığı yok, yalnızca çalıştırma gösterdi. |
| ~~**O117**~~ | **İmza anahtarı üç release turu kaybettirdi — PAROLASIZ anahtara geçildi (kabul edilmiş sapma)** | ✅ | ✅ anahtar `--ci` ile parolasız üretildi, `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` secret'ı **silindi** | ✅ Tehdit modeli §2/S4'e sapma, bedeli ve yeniden açılma koşuluyla birlikte yazıldı | **Üç turun her biri, ~25 dakikalık build'in EN SON adımında düştü ve her biri farklı bir sebeple.** (1) `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` ilk kurulumdan kalma, anahtar ise ikinci bir `signer generate` ile ezilmişti — çift eşleşmiyordu. (2) Anahtar yeniden üretildi, ama iki `gh secret set` komutundan **yalnız biri** koştu: zaman damgaları kanıtladı — parola 22:41, özel anahtar hâlâ **dünkü 12:45**. (3) İkisi de yeni anahtardan yazıldı ve **yine** uyuşmadı; parola değeri GitHub'a iki denemede iki farklı şekilde yanlış gitti. <br><br>**Neden parolasıza geçildi:** parola anahtar dosyasını yalnızca sahibinin diskinde korur; CI'ın kullandığı kopya zaten GitHub secret'ıdır ve parola oraya koruma **eklemez**, ikinci bir sır ekler. Bu depo kullanıcıya güncelleme yayınlamıyor (taslak sürüm `latest` olarak servis edilmiyor). Sapma sessizce geçilmedi — §2/S4'te bedeliyle ve *"gerçek dağıtım kararı alınırsa bu satır yeniden açılır"* koşuluyla duruyor. <br><br>**Ders:** bir sır, doğruluğu ancak onu kullanan sistemin en son adımında ölçülebiliyorsa, o sırrı **yerelde doğrulamadan** hiçbir yere yazma. Bu turda `Seed frontend/.env`'e eklenen bayt-uzunluğu teşhisi (defter O115) tam bu yüzden vardı ve `DESKTOP_RELEASE_ENV` için işe yaradı; imza anahtarının böyle bir teşhisi **yoktu** ve üç tur kaybettirdi. |
| ~~**O118**~~ | **İLK GERÇEK RELEASE — üç OS yeşil, imzalı manifest üretildi (2026-09-02)** | ✅ | ✅ | ✅ | Run **33571076974**, `desktop-v0.1.0`, taslak sürüm (GitHub bunu `latest` olarak servis **etmez**, yani kurulu hiçbir şey güncellenmedi — O111 riski tetiklenmedi). <br><br>**Üretilenler:** 14 varlık — Windows `msi` + `nsis`, macOS `dmg` + `app.tar.gz`, Linux `deb` + `rpm` + `AppImage`, **6 `.sig`**, ve **`latest.json`** (5328 bayt, 9 platform girdisi, hepsinde imza). <br><br>**Teknik lider'in doğrulaması ("success" yazması yetmez):** manifest indirildi; `windows-x86_64` girdisinin imzası `Syncra_0.1.0_x64-setup.exe.sig` ile **birebir aynı** (yer tutucu değil); imzanın ikili gövdesinden çıkarılan anahtar kimliği **`4CE97F70B4BEFE48`**, yani `tauri.conf.json`'daki pubkey'in ve `updater.rs`'in `EXPECTED_KEY_ID`'sinin tam karşılığı. `updaterJsonPreferNsis` de doğrulandı (`windows-x86_64` = `windows-x86_64-nsis`). <br><br>**Bu tur üç maddeyi birden kapattı:** O108 (`createUpdaterArtifacts` gerçekten `.sig` üretiyor), O116 (`uploadUpdaterJson` doğru isimmiş, `latest.json` var), O34 (24.04 derliyor — `libspa` sorunsuz, deb+rpm+AppImage üretildi). <br><br>**Ölçülmeyen:** bir güncellemenin gerçekten **kurulduğu**. İkinci bir sürüm ve onu çeken bir istemci gerekir; tek sürümle kanıtlanamaz ve öyle yazıldı. |
| ~~**O105**~~ | **§9/3 ve §9/4 GERÇEK KURULUMDA yeniden ölçüldü — KAPALI kalıyor, kanıt güçlendi** | ✅ | ✅ | ✅ | Önceki ölçümler dev ağacındaydı; bu kez **NSIS ile kurulmuş, oturumu açık** uygulama üzerinde ölçüldü. <br>• **§9/3 (DB düz `sqlite3` ile açılamıyor):** gerçek ayna `%APPDATA%\com.syncra.desktop\syncra\syncra.db` (644 KB + 402 KB WAL); ilk 16 bayt `41af 2f9b cb59 472c 387c 178d 9dc8 b51d` — rastgele şifreli veri, `SQLite format 3` başlığı **yok** (grep 0 eşleşme). <br>• **§9/4 (app data'da anahtar/token dosyası yok):** `%LOCALAPPDATA%\com.syncra.desktop` altında uygulamanın **kendi** dosyası yalnızca `logs\Syncra.log`. İsim taramasındaki 5 eşleşmenin **beşi de WebView2'nin kendi dosyaları** (`Vpn Tokens`, `Trust Tokens`, `TrustTokenKeyCommitments/keys.json`) — bizim değil. Device token keychain'de. <br>• **Yol bilgisi (F8 için gerekli):** ayna **Roaming**'de, log **Local**'de — F8'in taşıma prosedürü iki ayrı köke bakmalı. |
| ~~**O106**~~ | **JumpList'in görünmeyen yarısı: süreç AUMID'si — KAPANDI 2026-09-01 (O85 ile)** | ✅ | ✅ `run()`'ın ilk satırı `set_process_aumid()`; sabit üç yerde çivili (`jump_list.rs` testi `include_str!` ile `tauri.conf.json`'ı okuyor · `check-identifier.mjs` ikinci assert · kaynak-seviyesi sıralama testi) | ✅ Teknik lider kurulu pakette ölçtü: kısayol AUMID `com.syncra.desktop` **ve** Windows listeyi kabul etti (`CustomDestinations` 21 → 22) — iki kova artık aynı | Keşif turunun araştırması buldu, **teknik lider ölçerek doğruladı** — O85'in kodu yazılsaydı bile liste sessizce boş kalırdı. <br>• **Kısayol tarafı ✅:** Tauri'nin NSIS şablonu (`installer.nsi` → `SetLnkAppUserModelId`) kısayola `${BUNDLEID}`'yi yazıyor. Kurulu makinede ölçüldü: `…\Start Menu\Programs\Syncra.lnk` → AUMID **`com.syncra.desktop`**, hedef `…\AppData\Local\Syncra\syncra-desktop.exe`. <br>• **Süreç tarafı ❌:** `SetCurrentProcessExplicitAppUserModelID` **hiçbir yerde çağrılmıyor** (grep 0 — ne bizde, ne tao/wry/tauri kaynağında). Süreç, sistemin yoldan türettiği örtük AUMID ile koşuyor. <br>• **Sonuç:** görev çubuğu düğmesinin JumpList kovası ile `SetAppID` ile yazılacak liste **farklı kovalara** düşer. Belirti: hata yok, liste boş. MS belgesi şart koşuyor: *"must also assign the same AppUserModelID to all running windows or processes, shortcuts … and use that AppUserModelID when customizing its Jump List"*, üstelik **UI'dan önce**. <br>• **Çözüm:** `run()`'ın ilk satırında `#[cfg(windows)] SetCurrentProcessExplicitAppUserModelID("com.syncra.desktop")`. NSIS'e dokunmaya gerek yok. Yan fayda: `tauri-plugin-notification` toast'ları zaten `config().identifier` AUMID'siyle atıyor — süreç kimliği sabitlenince bildirim atıf/gruplaması da hizalanır. Sabit `check-identifier.mjs`'e ikinci assert olarak bağlanmalı. |
| ~~**O107**~~ | **JumpList gizlilik kancası — KAPANDI 2026-09-01, UÇTAN UCA ÖLÇÜLDÜ** | ✅ | ✅ `auth::logout` ve `storage::clear_local` → `jump_list::clear()` (`recent.json` + COM `DeleteList`) | ✅ Üç çağrı-yeri testi **artı** teknik liderin gerçek ölçümü: shell deposundaki `d34a3842cd2c11a0.customDestinations-ms` `clear()` sonrası **kayboldu** (22 → 21). Yani başlıklar OS'un düz-metin deposundan da siliniyor | O85 uygulanırsa kayıt başlıkları iki düz-metin yere yazılır: kabuğun kendi `recent.json`'ı ve OS'un `%APPDATA%\Microsoft\Windows\Recent\CustomDestinations` deposu. Ayna SQLCipher'lı (§9/3), ama bu iki kopya değil. <br>**Bu, F5'te kapatılan "wipe gizlilik açığı"nın birebir JumpList kopyasıdır** — o zaman yerel veri silinirken bir kopya geride kalıyordu. Bu yüzden O85'in kabulüne şu koşul bağlanıyor: `auth::logout` ve `storage::clear_local` yollarına `jump_list::clear()` (COM `DeleteList` + `recent.json` silme) **bağlanmadan madde kapanmaz**, ve elle doğrulama prosedürünün 7. adımı bunu ölçer. |

## 2. AÇIK — bilinçli ertelenmiş

| # | Madde | Neden ertelendi | Ne zaman |
|---|---|---|---|
| ~~**O6**~~ | **Kayıt satırı rozetleri — KAPANDI 2026-08-31** | ✅ Rozet `desktop/src/ui`'dan `frontend/src/components/shared/`'a taşındı; `sync_state` tanımsız/`synced` ise `null` döner, yani web'de platform dallanması olmadan görünmez | ✅ 8 liste sayfası (deal, contact, company, lead, task, ticket, quote, activity); kapsam manifest'teki `kind:'mutate'` fiillerinden türetildi, `mutate`'i olmayan varlığa rozet konmadı | ✅ Web izolasyonu üç halkalı: `web.ts` 0 eşleşme, backend Resource 0 eşleşme, bileşen erken `return null`. `i18n:dead-keys` 95 → **93** (`recordBadge.*` canlandı). **Not:** statik/tip düzeyinde doğrulandı, uygulama koşturularak değil — o F4 senaryosunun işi. |
| ~~**O7**~~ | **`boardApi` → `DataSource` — KAPANDI 2026-08-31** | ✅ `DealsSource`'a `board`/`move` (124 → **126** metot) | ✅ Web uçları artık yalnız `platform/web.ts`'te (birebir taşındı); masaüstünde `board()` aynadan `BoardResponse` kuruyor, `move()` HTTP atmadan outbox'a `deal.move` + `to_stage_id` yazıyor | ✅ `check-data-wiring` 126/126, EXIT=0 |
| ~~**O8**~~ | **`storage::settings` getter — KAPANDI 2026-08-31** | ✅ | ✅ `storage_settings` komutu dört kaynakta (`storage.rs:38`, `lib.rs:107`, `commands.ts:160`, kontrolör `CONTRACT`); `StorageSettings.tsx` `localStorage` aynasını bıraktı, motordan okuyor | ✅ `settings_reflects_a_write_without_a_restart` — mevcut `settings_are_persisted` yalnız restart-sonrası kalıcılığı kanıtlıyordu, aynı-oturum simetriyi değil |
| ~~**O9**~~ | **Üç hook refcount desenine geçti — KAPANDI 2026-08-31** | ✅ | ✅ `useDashboardSocket`, `useActivityStream`, `usePresence` artık `acquireChannel`/`releaseChannel` kullanıyor; `DashboardPage.tsx` ve `LiveStreamTab.tsx`'in bayat `echo.leave()` yorumları düzeltildi | ✅ 4 senaryoluk refcount probu: paylaşılan kanalda ilk release **bırakmıyor**, son release bırakıyor; presence aynı; bağımsız kanallar birbirini etkilemiyor; Echo yokken `null` dönüp çökmüyor |
| ~~**O10**~~ | **`docs/DESKTOP-OFFLINE-TEST.md` yazıldı ve senaryo KOŞULDU — 2026-08-31** | Belge tekrarlanabilir hâlde: üç portun gerekçesi, kurulum komutları, CDP sürüş yöntemi ve **sınırı**, 20 işlemin tablosu, çakışmaların nasıl üretildiği, ölçüm yöntemi, 5 bulgu + kök neden kanıtları. **Senaryo F4 kabulünü GEÇMEDİ** — bkz. O45, O46. |
| ~~**O11**~~ | §9/5 (deep link) ve §9/6 (clipboard) — **İKİSİ DE DEĞERLENDİRİLDİ ve KAPANDI** | §9/5 F5'te fuzz'la (83 örnek) kapandı; §9/6 2026-09-01'de bağımlılık katmanı dahil ölçüldü, 5 test + 4 negatif kontrol | KAPANDI 2026-09-01 |
| ~~**O12**~~ | 🔴 **Updater bloğu yoktu → paketlenmiş uygulama HİÇ AÇILMIYORDU — KAPANDI 2026-09-01, ÖLÇÜMLE** | ✅ | ✅ Kullanıcı minisign çiftini üretti (**`4CE97F70B4BEFE48`**, özel yarısı repoda değil, `TAURI_SIGNING_PRIVATE_KEY(_PASSWORD)` secret'larında — `gh secret list` ile doğrulandı). `tauri.conf.json` `plugins.updater`: GitHub Releases `latest/download/latest.json` + `pubkey` + `windows.installMode: "passive"`. `lib.rs`'teki `#[cfg(not(debug_assertions))]` **silindi** — kayıt artık koşulsuz | ✅ Teknik lider ölçtü: `npm run tauri -- build --no-bundle --features custom-protocol` → `Finished release profile in 1m 14s`, **EXIT=0**; exe başlatıldı → **10 sn sonra ayakta** (PID doğrulandı). Aynı binary önceki turda `PluginInitialization("updater", …)` ile **exit 101** veriyordu | **`cfg`'nin silinmesi düzeltmenin kendisi kadar önemli:** CI `--debug` derliyor, `debug_assertions` açık kalıyor, yani `cfg(not(debug_assertions))` blokları CI'da **hiç derlenmiyordu** — bu panik sınıfını hiçbir CI job'ı yakalayamazdı. Kayıt koşulsuz olduğu için artık her build aynı plugin-init yolundan geçiyor. Aşağısı tarihçedir: **Tahmin değil, ölçüm.** F7'de `tauri build` bu projede ilk kez koştu: build **başarılı**, MSI+NSIS üretildi — ama çalıştırınca:<br>`PluginInitialization("updater", "Error deserializing 'plugins.updater': invalid type: null, expected struct Config")` · **exit 101**.<br>`lib.rs:81` updater'ı `#[cfg(not(debug_assertions))]` ile kaydediyor, `tauri.conf.json` `plugins` yalnız `{"deep-link":…}` içeriyor. <br><br>**CI bunu YAPISAL OLARAK yakalayamaz:** `desktop-ci.yml:431` `build --debug` koşuyor, `--debug` `debug_assertions`'ı açık bırakıyor, o `cfg` bloğu **CI'da hiç derlenmiyor**. `lib.rs:78-80` yorumu bunu zaten yazmış — **öngörülmüş ama hiç koşulmamış.** <br><br>**Karar (danışman):** endpoint **GitHub Releases** — `releases/latest/download/latest.json` (`desktop-release.yml` zaten `includeUpdaterJson: true` ile onu üretip imzalıyor; ayrı sunucu **kurulmayacak**), `windows.installMode: "passive"`. Boş endpoint listesi **reddedildi**. SYNCDESKTOP §6.5 buna göre yeniden yazıldı. <br><br>**KULLANICI AKSİYONU — 5 dakikalık ama tam blocker:** `npm run tauri -- signer generate -w <güvenli-yol>`; public key `tauri.conf.json`'a, private key + parola GitHub secret'ına. Sahte pubkey **yasak** (`lib.rs:75-77` bilinçle reddediyor). Anahtar gelince blok girilir ve `cfg` **silinir**. Bu gelene kadar O85/O86/O87'nin paketli doğrulamaları ve oturum-açık RAM ölçümü **yapılamaz**; ama CI smoke, `rust-version`, apt ve sözleşme düzeltmeleri anahtarsız ilerler. |
| ~~**O13**~~ | ~~Linux/WebKitGTK **sıfır ölçüm** (D-4 dahil)~~ | **KAPANDI 2026-08-31** — WSL2 Ubuntu 26.04 + rustc 1.98.0 + WebKitGTK 2.52.3 kuruldu, D-4 Linux'ta ölçüldü (§4 D-4 satırı). Kalan Linux borcu artık O13 değil, **O23** (CI ubuntu-22.04 ↔ yerel 26.04 sürüm farkı). |
| ~~**O14**~~ | **CI ilk kez koştu ve YEŞİLE DÖNDÜ — 2026-08-31** | Beş işin beşi de success: `backend (php artisan test)`, `rust workspace (ubuntu-24.04 / macos-latest)`, `frontend`, `cargo audit`. (`windows-latest` vendored OpenSSL derlemesi nedeniyle uzun sürüyor, ayrı izleniyor.) İlk koşum iki gerçek hata çıkardı (O32, O33) — ikisi de düzeltildi ve **aynı SHA üzerinde** doğrulandı. |

## 3. BELGE BORCU

> **Bu defterin iki kaydı yanlıştı (2026-08-31, W2-D ortaya çıkardı).** İkisi de teknik liderin
> yazdığı satırlardı ve **şerit prompt'larına aynen kopyalanmıştı**, yani yanlış bilgi iş
> tanımına dönüşmüştü:
>
> - **O8** "crate'te settings getter yok" diyordu. Yanlış — `SyncEngine::settings()`
>   (`sync/mod.rs:866`) önceki bir fazda eklenmiş ve test edilmişti; hiçbir Tauri komutuna
>   bağlanmamıştı sadece. `StorageSettings.tsx`'in başındaki yorum da aynı bayat iddiayı
>   taşıyordu — muhtemelen defter oradan yazılmıştı.
> - **O9** "3 hook'ta ham `echo.leave()` **+ 5 sn watchdog** birlikte yaşıyor" diyordu.
>   Watchdog kısmı kodda **hiç yoktu**; şerit aramaya gönderildi ve bulamadı.
>
> **Ders:** bu defter kanıt belgesi olduğunu iddia ediyor (`dosya:satır` sütunu var), ama bu
> iki satır kanıtsız yazılmıştı. Bir madde yazılırken iddiası **o an** grep'le doğrulanmalı;
> "muhtemelen böyledir" bir sonraki şeridin görev tanımı hâline geliyor.


| # | Madde | Not |
|---|---|---|
| ~~**O15**~~ | **`SYNCDESKTOP.md` revize edildi — KAPANDI 2026-08-31** | SPEC-1 turu: 390 ekleme / 44 silme, yerinde cerrahi düzeltme + **§13 REVİZYON GÜNLÜĞÜ** (26 çapa). D1–D13, P1–P20, S1–S10, A19/A25–A28 işlendi; §6.2'ye `handle_realtime`, §6.3'e CSP, §4.1'e RO pencere muafiyeti eklendi. Kodda karşılığı olmayanlar şartnamede 🔴 **AÇIK** işaretli. |
| ~~**O16**~~ | **Mimari belge konsolidasyonu — KAPANDI 2026-08-31** | Belge yeniden yazılmadı (EK'ler karar tutanağıdır, silmek tarihi silmek olurdu). Yerine: **§0.1 "Gövde Bayatlık Haritası"** — bağlayıcı, çakışmada hangi tarafın kazandığını söylüyor; üç bayat iddia **yerinde** düzeltildi (§4.2 `watch.ignored` tek glob → üç glob, gövde koddan da sapmıştı; §3.7 "8 dosya" → 6+26+3; §11'in sekiz "açık kararı" → sekizi de kapalı, kanıtlarıyla). Kalıcı kural yazıldı: *"gövdeye yeni iddia eklenirken onu geçersiz kılan bir EK varsa **gövde düzeltilir**"*. 1389 → 1435 satır, tablo uyuşmazlığı 0. |
| ~~**O17**~~ | **`docs/PROGRESS.md` güncellendi — KAPANDI** | Başına desktop durum bloğu eklendi; oturum başı okuma artık doğru bağlamla başlıyor. |

## 4. ÖLÇÜM BORCU

| # | Madde | Not |
|---|---|---|
| **O18** | P4a'nın iki uzun transaction ölçümü | `PipelineStageService::deactivate` ve `ChatReadState::fanOutNewMessage` — protokol §2.4 istiyor, hiçbir belgede iz yok. `sync_counter` küresel mutex'inin en riskli müşterisi ölçülmemiş. *(F1 raporu `fanOutNewMessage` için 2.6–4.0 ms verdi; `deactivate` için 7.5 ms/deal. Bu satır o ölçümlerin protokol belgesine işlenmediğini kaydediyor.)* |
| ~~**O23**~~ | **CI Linux hedefi — KAPANDI 2026-08-31** | `desktop-ci.yml` 5 yerde `ubuntu-22.04` → **`ubuntu-24.04`** (keşif turu kararı: 22.04'ün webkit2gtk-4.1'i D-4 ölçüm ortamından iki nesil geride ve imaj emekliliğe yaklaşıyor; `ubuntu-26.04` runner'ı GA değil). Yeşil koşumla doğrulandı. **`desktop-release.yml` bilinçli olarak 22.04'te tutuldu** — bkz. O34. |
| ~~**O24**~~ | **`identifier` kalıcı depolama anahtarı — KAPANDI 2026-08-31** | ✅ | ✅ `desktop/scripts/check-identifier.mjs` `com.syncra.desktop`'a sabitliyor | ✅ negatif kontrol: değer bozulunca exit 1, geri alınınca exit 0 (`tail` değil, exit koduyla ölçüldü) |
| **O19** | **Uçtan uca doğrulama** | Tüm testler mock-simetrik (wiremock ↔ PHPUnit). İki mock'un aynı wire gerçeğini tarif ettiği hiç kanıtlanmadı. INT-1 şeridi bunu ilk kez deniyor. |
| ~~**O31**~~ | **Ölü anahtar tabanı 129 → 83 — YANLIŞ POZİTİFLER TEMİZLENDİ 2026-09-01** | ✅ | ✅ **2a:** `notificationText.ts` anahtarı şablon literaliyle kuruyor → kontrolörün önek dalı (`check-i18n-dead-keys.mjs:207`) yakalıyor, **36** düştü. **2b:** `EXTERNAL_CONSUMER_PREFIXES` allowlist'ine `desktop:tray.` → **10** düştü | ✅ **Kalan 83'ün tamamı `desktop.json`** ve gerçek F4/F5 envanteri: `fields.*` 55 · `onlineOnly.*` 17 · `sync.*` 4 · `conflicts.*` 4 · `window.closeToTray.*` 3. F5 kapanışında bu sayı düşmeli. |

---

## KAPANMIŞLAR (üç sütun da ✅)

| Madde | Kod | Test |
|---|---|---|
| A11 realtime köprüsü | `bridge/realtime.ts`, `commands/sync.rs`, `lib.rs` | `check-realtime-wiring` + 4 negatif senaryo |
| A19 `DataSource` fiil-bazlı | `platform/data/*` (124 metot) | `check-data-wiring` + negatif test |
| A26 sunucu tarafı | `SyncPullService::attachTicketSla()` | `SyncPullTicketSlaTest` |
| D-6 açılış kapısı desktop'ı kapsıyor | `check-i18n-bootstrap.mjs` 1h bölümü | negatif test (boz→kırmızı, düzelt→yeşil) |
| §9/9 log PII maskeleme | `src-tauri/src/logging.rs` | 4 test + negatif test |
| `Echo.leave()` refcount (6 hook) | `lib/channelRegistry.ts` | refcount probu + negatif kontrol |
| P19 `price_list_items` tombstone | `SyncDeletionObserver` + registry | 2 test |
| P20 `deal.move` → `to_stage_id` | `tests/push_flow.rs:81` | crate testi |
| O5 komut adı üçlü tutarlılığı | `storage_stats` üç tarafta | `check-command-wiring.mjs` + iki yönlü negatif kontrol |
| A25 403 `USER_DEACTIVATED` → wipe | `sync/mod.rs:1072`, `protocol.rs:372` | `wire_contract.rs` 3 test (2 negatif kontrol) |
| TM-F2 `sync_deletions.owner_key` | `SyncDeletionObserver`, `SyncPullService:542` | BE-K2 hedefli testler |
| O20 `desktop.logout.*` ödünç anahtarlar | `LogoutConfirm.tsx:42` | `i18n:check` — 4 dilde `title/description/discardWarning_{one,other}/force` |
| U2 device token webview'e akıyor | `platform/auth.ts:191` `setDeviceToken(options.token)` | WIRE-1 CDP yakalaması: `/api/deals/board` → `Bearer` ile 200 |
| D-4 `localStorage` kalıcılığı (**Windows + Linux**) | ölçüldü, karar: 3 dosya dokunulmuyor, `Platform.storage` eklenmiyor | Windows: WebView2. Linux 2026-08-31: WSLg + WebKitGTK 2.52.3, 4 koşumluk prob — süreç yeniden başlatmada **ve** tüm süreç ağacının `SIGKILL`'inden sonra değer korundu. Depo: `~/.local/share/com.syncra.d4probe/localstorage/tauri_localhost_0.localstorage` (SQLite+WAL). Origin dizesi platforma göre değişiyor (Linux `tauri://localhost`, Windows `http://tauri.localhost`) → kova paylaşılmaz, beklenen davranış. |

---

# 5. AUTH-1 UYUŞMAZLIK LİSTESİ (ilk gerçek uçtan uca koşum, 2026-08-31)

> Bu 16 madde, masaüstünün gerçek backend'e karşı ilk kez çalıştırılmasıyla ortaya çıktı. **Hiçbiri birim testleriyle görünmüyordu** — wiremock (crate) ile PHPUnit (backend) birbirine karşı yeşildi, gerçek wire'a karşı değil. RISK-1 denetiminin R1 maddesi tam olarak bunu öngörmüştü.

## 5.1 Yapısal blokörler

| # | Madde | Karar | Kod | Test | Kanıt |
|---|---|---|---|---|---|
| **U1** | `device_fingerprint` biçimi — masaüstü **hiçbir koşulda giriş yapamıyor** | ✅ | ⏳ FIX-RUST | ❌ | `src-tauri/src/commands/auth.rs:140` `Uuid::new_v4()` = 36 karakter tireli; `DeviceTokenRequest.php:38` `size:64` + `^[0-9a-f]{64}$` istiyor → 422. Backend dosyasında **neden** 64 hex olduğunu açıklayan yorum var. |
| **U3** | `window_days: 0` — **her senkron turunun pull yarısı 422** | ✅ | ⏳ FIX-RUST | ❌ | `crates/.../sync/mod.rs:337` `pull_until_drained(…, 0, None)`; sunucu `min:1`. §4.4 zaten "delta'da filtre yok" diyor — alan hiç gitmemeliydi. Canlı: `window_days:0`→422, `30`→200. |

## 5.2 İşlevi kıran uyuşmazlıklar

| # | Madde | Durum | Etki |
|---|---|---|---|
| **U2** | Cihaz token'ı webview'e hiç ulaşmıyor — `Session`'da `token` yok, hiçbir komut vermiyor | ⏳ FIX-RUST | §8 online-only uçları, `/api/broadcasting/auth`, `/api/password/change` kimliksiz gidip **401** alıyor. AUTH-1 `installUnauthorizedGuard()` ile 401'i yutmak zorunda kaldı — **bu bir yama, çözüm değil**; U2 kapanınca gözden geçirilmeli. |
| **U4** | `list_devices` `{data:[...]}` zarfını ayrıştırmıyor | ⏳ FIX-RUST | **Cihazlar ekranı hiç açılmıyor**; "Bu cihaz" rozeti kabul kriteri karşılanamadı. Sunucu `is_current:true`'yu doğru üretiyor. |
| **U5** | `restore` çevrimdışı çalışmıyor (`load_manifest(true)` her seferinde ağ) | ⏳ FIX-RUST | AUTH-1 webview'de `localStorage`'da kimlik kopyası tutmak zorunda kaldı — **düz metin PII** (token değil). U5 kapanınca silinmeli. |
| **U6** | `logout` sunucudaki token'ı iptal etmiyor | ⏳ FIX-RUST | Çıkıştan sonra token DB'de canlı kalıyor (`id=5`, `last_used_at` ile doğrulandı). |
| **U15** | `bootstrap` komutu `generate_handler!`'da yok | ⏳ FIX-RUST | `BootstrapProgress` yayan tek yol erişilemez; AUTH-1 ilerlemeyi `download_archive(0)` + 400 ms'lik sayım anketiyle **taklit etmek** zorunda kaldı. |
| **U11** | Hata kodları mesaj string'ine gömülüyor; `retry_after` kayboluyor | ⏳ FIX-RUST | `transport.rs:78` `SyncError::Validation(kod)`; `auth.ts` kodu regex'le çıkarıyor — kırılgan. `LOCKED_OUT`'un geri sayımı masaüstünde **çalışamaz**. |
| **U12** | 422 doğrulama hataları `PROTOCOL_ERROR` oluyor | ⏳ FIX-RUST | `device_login` yalnız 401/403/423 ayrıştırıyor. U1'in teşhisini zorlaştıran şey buydu. |

## 5.3 Veri kapsamı ve tüketim

| # | Madde | Durum | Etki |
|---|---|---|---|
| **U7** | Kanban tamamen boş — `boardApi` hâlâ HTTP | ❌ | **O7 canlıda doğrulandı.** Token olmadığı için 401 → 5 boş kolon. Aynı ekranın liste görünümü 18 fırsatı yerel aynadan basıyor. U2 kapanınca 401 kalkar ama board yine **online-only** kalır; F4'ün Kanban offline move maddesi buna bloklu. |
| **U8** | SLA sütunu `—` — `mappers.ts` A26'yı tüketmiyor | ❌ | **O2 canlıda doğrulandı.** Sunucu dört alanı gönderiyor; istemci hâlâ A23'ün `null`/`0` davranışında. Başlıkta "SLA breaches: 15" derken her satır boş. |
| **U9** | Pull penceresi ilişkili kayıtları kapsamıyor | ⏳ FIX-BE | 18 fırsat geldi ama **1 firma** → Fırsatlar'da **18/18** satırın "Company"si `—`. Sözleşme boşluğu: §4.4 bootstrap penceresini tanımlıyor, ilişkisel bütünlükten söz etmiyor. |
| **U10** | `tags` aynası 0 satır | ⏳ FIX-BE | Fırsat satırları `tags:[9]` taşıyor ama tablo boş → her ekranda "Tags: —". Bootstrap %93'te duruyor (15 tablodan 14'ü) — sayaç dürüst, veri eksik. |

## 5.4 UX ve sözlük

| # | Madde | Durum |
|---|---|---|
| **U13** | Restore edilen oturum `/login`'de kilitleniyor — `LoginPage`'de "zaten girişli" muhafızı yok | ⚠️ Masaüstü tarafında `leaveLoginRoute()` ile kapatıldı; kalıcı çözüm `frontend/**` |
| **U14** | Eksik `desktop.*` anahtarları | ✅ FIX-I18N kapattı — 199 → **206**, dört dil birebir |
| **U16 / O1** | A25 (403 `USER_DEACTIVATED` → wipe) | ⏳ FIX-RUST — U11 ile birlikte kapanıyor |

## 5.5 YENİ AÇIK — anahtar var, tüketen yok

**O20.** FIX-I18N'in 7 girdisi (`desktop.logout.*`, `errors.INVALID_CREDENTIALS`, `errors.PENDING_MUTATIONS`) eklendi ama **hiçbiri kullanılmıyor** — AUTH-1'in `LogoutConfirm` bileşeni hâlâ `common:layout.logout` + `desktop:sync.pendingChanges` + `desktop:storage.clearLocal.description` ödünç alıyor. Sonuncusu logout'un yerel DB'yi wipe etmesi bakımından kısmen doğru ama **"gönderilmemiş iş kaybolacak" demiyor** — `discardWarning` tam bunun için yazıldı.

Aynı desen I18N-B turunda da olmuştu: `desktop.entities.*` (22) ve `desktop.fields.*` (55) eklendi, `ConflictInbox`/`PendingRecords` hâlâ ham tablo/kolon adı basıyor.

> **Ders:** sözlük şeridi ile tüketici şerit ayrı olduğunda anahtarlar **ölü doğuyor**. Bundan sonra i18n görevleri ya tüketici şeritle aynı şeride verilmeli, ya da tüketim ayrı bir madde olarak deftere yazılmalı. Bu iki grup (O20) şu an ikinci durumda.

## 5.6 Bırakılan durum — kullanıcı onayı bekliyor

AUTH-1, U1 kapanana kadar login'in **tek çalışma koşulu** olarak Windows Credential Manager'a `device-fingerprint.syncra-desktop` adlı 64-hex bir kayıt elle tohumladı.

> **DÜZELTME (RISK-2 denetimi).** Bu madde önce "FIX-RUST U1'i kapatınca gereksizleşecek" diyordu — **yanlıştı.** `auth.rs`'in onarım mantığı yalnız **geçersiz** bir değeri değiştiriyor (`if is_valid_fingerprint(&existing) { return Ok(existing) }`); elle tohumlanan kayıt geçerli 64-hex olduğu için **kalıcı olarak korunur**. Değer şerit transkriptlerinden ve raporlardan geçmiş; fingerprint bir sır değil ama entropisi ve geçmişi bilinmeyen bir cihaz kimliğini kalıcılaştırmanın gereği yok.

**Yapılacak (elle, FIX-RUST indikten sonra):**
```
cmdkey /delete:device-fingerprint.syncra-desktop
```
Silinince uygulama bir sonraki girişte CSPRNG'den taze 64-hex üretir. Sunucuda eski fingerprint'e bağlı cihaz satırı öksüz kalır — Cihazlar ekranından iptal edilebilir.

## 5.7 U9/U10'un KAPSAM DIŞI KARDEŞLERİ (FIX-BE raporladı, dokunmadı)

**O21 — RO tabloları pencereleniyor, oysa §4.1 onları referans grubunda sayıyor.**

FIX-BE `tags` için pencere muafiyeti eklerken aynı kök nedenin başka tablolarda da yaşadığını ölçtü:

| Tablo | 30 günlük pencerede | Toplam |
|---|---|---|
| `products` | **0** | 20 |
| `users` | 7 | 10 |
| `tags` (düzeltildi) | 0 → **12** | 12 |

`SYNCDESKTOP.md` §4.1 bu RO tabloları zaman-sıralı ana varlık grubundan (`companies…quotes`) **ayrı**, referans/lookup grubunda listeliyor ve yalnız `exchange_rates` için açık "(son 7 gün)" notu taşıyor. Yani sözleşme bunların pencerelenmesini **öngörmüyor**; kod pencereliyor.

**Somut etkisi:** AUTH-1'in bootstrap'inde `users=7` geldi. Eksik 3 kullanıcıya atanmış kayıtlarda "Atanan: —" görünür — U9'un firma sorununun aynısı, farklı kolonda. `products=0` ise teklif kalemlerinin ürün adlarını boşaltır.

**Ayrıca:** `users` için `assigned_to`/`owner_id` üzerinden U9 tipi bir ilişkisel kapanış da gerekebilir (FIX-BE teyit etmedi, kapsam dışı bıraktı).

Karar ❌ · Kod ❌ · Test ❌ — kapsam §0.5 gereği FIX-BE'nin görevine dahil değildi, ayrı bir tur gerektiriyor.

**O22 — U9 kapanışının bütçe kenar durumu.** Bir pull, 5 referans tablosundan bazılarını 5 MB bütçe dolduğu için hiç işleyemezse, o tablonun kapanış adayları kaçar ve `companies` cursor'ı 0'ın üstüne çıktığı için **bir daha denenmez**. Mevcut veri setinde ulaşılamaz (payload/bütçe oranı %0.28) ama büyük kurumsal veri tabanında gerçek bir sınır. FIX-BE bunu bilinçli kabul edip raporladı.
